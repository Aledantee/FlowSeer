# Captured solutions

Solutions preserve verified lessons that are easy to miss by reading the happy
path. Use each document's `applies_when` frontmatter to decide whether to read
it. Category and severity help with search and review priority; they do not make
a solution override an accepted architecture record or binding convention.

| Solution | Read when |
| --- | --- |
| [SNMP Collection Library: Architecture and Fast-Path Conventions](architecture-patterns/snmp-collection-library-architecture-and-fast-path-conventions.md) | Changing `src/protocol/snmp`, generated MIB bindings, the SNMP hot path, or its conformance and performance gates. |
| [A Decoder's Decline Costs the Whole Table, Not the Field](architecture-patterns/decode-failure-blast-radius-in-generated-walks.md) | Tightening a generated-column decoder, choosing error versus coercion for malformed agent data, or investigating a table that unexpectedly returns no rows. |
| [Key Resolution Degrades and Reports, It Never Fails a Row or a Run](architecture-patterns/key-resolution-degrades-and-reports-never-fails.md) | Changing INDEX/AUGMENTS resolution, mibgen key-type emission, `DecodeIndex`/`DecodeIndexInto`, or a mapper's use of `KeyValid`. |
| [The errs Package: FlowSeer's Owned Error Type and Its Conventions](architecture-patterns/errs-package-architecture-and-error-conventions.md) | Creating or exposing Go errors, adding an `errs.Code`, or deciding whether diagnostic data belongs in an error, attribute, or log. |
| [Declaration-Level SMI Recovery Preserves Declared Semantics](architecture-patterns/gosmi-drops-bits-member-numbers.md) | Changing SMI parser recovery, BITS member handling, MIB generation, or use of gosmi as a reference. |
| [A Panic-to-Error Conversion That Reaches an API the Unit Did Not Scope Is a Re-Plan, Not a Wider Diff](architecture-patterns/panic-to-error-conversion-that-escapes-its-unit-is-a-replan.md) | Converting a sanctioned panic to an error return, or adding an error return to a function that had none, as a bounded refactor unit; the new error's propagation reaches a core or exported API the unit did not name; a refactor unit's diff keeps growing because each converted site forces its callers to change; choosing between threading a new error up and containing the failure where it arises. |
| [Local Bus Durability Is a Runtime Setting, Not Storage Identity](architecture-patterns/local-bus-durability-is-a-runtime-setting-not-storage-identity.md) | Changing `BusConfig` or the embedded JetStream server options, adding a field to the runtime manifest, choosing a durability policy for a service, a service that refuses to start with `service/bus-config` because no fsync policy is declared, or investigating a handler that ran twice after a power cut. |
| [Trace Context Relays Through Trace-Disabled Modules](architecture-patterns/trace-context-relays-through-trace-disabled-modules.md) | Adding a publish or delivery path or a telemetry helper in `src/common/service`, choosing parent versus link at a durable boundary, or debugging a trace that breaks where one module has traces disabled. |
| [Documenting Intentional Protobuf Validation Deviations](conventions/document-intentional-schema-deviations-with-comment-and-test.md) | Deliberately departing from a package-wide validation pattern or preserving forward compatibility for enum and repeated-field validation. |
| [A Signature Over a Protobuf Message Covers Carried Payload Bytes, Never Re-Serialized Fields](conventions/sign-protobuf-payload-bytes-never-fields.md) | Adding a signature, MAC, or proof of possession over a protobuf message, writing a verifier that looks up its key by an id inside the signed message, or reviewing text that says a signature is "over the serialized fields". |
| [A Self-Redacting Type Redacts Only Where fmt Consults Its Methods](conventions/a-self-redacting-type-redacts-only-where-fmt-consults-it.md) | Adding a field that carries a password, passphrase, private key, or community string; writing a type whose `String`/`Format`/`LogValue` hides what it holds; claiming a value cannot leak into a log, error, or test failure; or printing a struct that keeps a `secret.Value` in an unexported field. |
| [An Expect-Style Prompt Scanner Must Reset Its Window Before Each Command, Not Just Strip Matches After](architecture-patterns/expect-style-prompt-scanner-must-reset-its-window-per-command.md) | Implementing or reviewing an expect-style command/response loop over a persistent shell (SSH, serial, Telnet) that matches a caller-supplied prompt regex against accumulated output. |
| [A src/protocol/ssh Prompt Pattern Needs a Multiline Anchor and Must Exclude Every Sibling Prompt's Shape](architecture-patterns/ssh-prompt-patterns-need-multiline-anchors-and-must-exclude-siblings.md) | Writing or reviewing a new `Prompt.Pattern` for `src/protocol/ssh.Command`, especially when two session states (privilege levels, configuration modes) look alike except for one substring. |
| [A Generated Table.Walk Calls Session.GetBulk/GetNext Directly, Never BulkWalk or BulkWalkRaw](architecture-patterns/generated-table-walkers-call-getbulk-not-bulkwalk.md) | Writing a test double that wraps an `snmp.Session` to count, record, or fail calls for a specific table, or reviewing a test that asserts on "how a table was walked". |
| [A Mounted Credential Read Needs True O_NOFOLLOW, and Its Sidecar Metadata Must Be Pinned to the Same File](architecture-patterns/mounted-credential-reads-need-openat-nofollow-and-atomic-pairing.md) | Reading a secret from a file another process can replace; choosing between `os.Root`/`os.OpenInRoot` and a raw `O_NOFOLLOW` open; splitting a credential's material and its version into a data file plus a sidecar. |
| [What Releases or Classifies State in Another Service Is That Service's Condition, Not the Nearest Thing the Sender Holds](architecture-patterns/a-cross-service-signal-carries-the-receivers-meaning.md) | Handing another component a callback that releases state (a Confirmer, an acker, a completion signal) where the two track different conditions; writing a doc comment whose precondition the function does not check; filling a required error-code or status field for a failure you could not classify; or reviewing a seam where both sides' tests pass and the question is whether they mean the same thing by what crosses. |
| [A Single-Active-Drainer Queue Must Not Leak the Drainer's Context or Its Shutdown Flag Onto Other Waiters](architecture-patterns/single-active-drainer-must-not-leak-its-context-or-its-shutdown-flag.md) | Implementing or reviewing a queue where one caller's goroutine becomes the sole active worker (a `sync.Mutex.TryLock`-guarded drain loop) for other callers' items; adding a Close/Shutdown method to a type that both admits new work and delivers external signals to work already admitted; a worker loop that can unlock between an empty queue check and a new admission; request coalescing across callers with different deadlines; or a method delivering one external signal to a single waiter that may already have received it. |
| [A Per-Account JetStream Disk Budget Is a Reservation Against the Server's Store Ceiling](architecture-patterns/per-account-jetstream-disk-budgets-reserve-against-the-server-store-ceiling.md) | Changing the hub's store ceiling or its per-account disk budgets, adding a JetStream-enabled account, sizing a deployment's edge count against a fixed disk, or an edge attach that fails with `jetstream not enabled` after an earlier edge attached fine. |
| [A Restarted Hub Must Re-Attach Every Persisted Edge Account, or Its Leaves Cannot Authenticate](architecture-patterns/a-restarted-hub-must-re-attach-every-persisted-edge-account.md) | Changing `StartHub`'s startup sequence or the per-edge key files, adding per-edge state created on first attach, swapping the nats account resolver, a leaf rejected with an authentication error on an unrotated credential, or reviewing a credential-minting component whose tests never restart it. |
| [A golang.org/x/net/bpf RawInstruction Compiles Against bpf.NewVM but Always Fails to Build](conventions/bpf-rawinstruction-never-satisfies-newvm.md) | Passing a compiled BPF program to `bpf.NewVM` anywhere it already exists as a `[]bpf.RawInstruction`; a kernel-attached filter and a userspace-run filter must both execute the same compiled program; debugging a "must end with RetA or RetConstant" error against a program that visibly does. |
| [Validate and Derive Judge What New Builds, Not What the Caller Wrote](architecture-patterns/validate-and-derive-judge-what-new-builds.md) | Writing a constructor that fills a zero field with a default or an assigned value and a `Derive`, `Diff`, or `Compare` that reads it; building a lookup map from a name nested inside a container and the `Validate` meant to keep it unique; carrying an assigned value into a derived instance whose new configuration may claim it; or a derived simulator state that loses roles, timers, or entries for an unchanged configuration. |
| [Attributing pump.TrySendDropOldest Losses to a Specific Batch Requires Capping Your Own Tracking FIFO to the Pump's Buffer Size](architecture-patterns/attribute-pump-drop-oldest-losses-cap-your-own-fifo.md) | Building a drop-oldest streaming producer on `src/common/pump.Pump[T].TrySendDropOldest` that must attribute a loss to a specific value, not just count it; a drop-accounting counter that looks right under a light load test but drifts under sustained throughput. |
| [Stopping a Pump Does Not Close Its Data Channel or Release a Forwarder](architecture-patterns/stopping-a-pump-does-not-close-its-data.md) | Combining pumps or other streams where stopping a producer leaves its data channel open; waiting for forwarders after a sibling source fails or the downstream consumer stops; investigating a test that hangs after a producer observes stop. |
| [A Static Scan Cannot Decide What a Function Does With Its Arguments; Prove That By Execution](conventions/prove-a-body-property-by-execution-not-by-ast-reading.md) | Writing a `go/ast` check that must decide something about a function body rather than a call expression, exempting a call because an enclosing function is "known" to behave a certain way, judging whether a scan's zero findings mean the property holds or that it cannot see the violation, or reviewing a second narrowing of a syntactic predicate that already missed a shape. |
| [A Gate Selected By Name Stops Running Silently; Enumerate It and Count What Ran](conventions/a-gate-selected-by-name-stops-running-silently.md) | Wiring a test package into a hook, script, or CI by name or with `go test -run`, adding an entry to a list of checks a runner iterates, deciding whether a gate that reported success actually ran, or reviewing a fix for a check that stopped checking to see whether it removed the failure or moved it up one level. |
| [A Reflection Perturbation Gate Must Copy Unexported-Field Structs Whole and Assert the Change It Induced](conventions/a-perturbation-gate-that-copies-structs-by-field-passes-vacuously.md) | Writing or reviewing a reflection-driven coverage or property gate that deep-copies a value, perturbs one leaf, and asserts a Diff or validator reacted; deep-copying a struct with unexported fields through reflection; or asserting a checker "reported a change" without confirming the change names the leaf you perturbed. |
| [A State Fingerprint Used as a Convergence Oracle Lies When It Omits a Resolution Axis or Keeps a Timer](conventions/a-convergence-fingerprint-lies-when-it-omits-a-resolution-axis-or-keeps-a-timer.md) | Building or extending a canonical state fingerprint or hash that a caller compares across steps to decide convergence, oscillation, or run equality; deciding which snapshot fields belong in it, especially state that resolves over time; or reviewing a reflection walk that classifies fields include/exclude without proving the included ones reach the output. |
| [A Stateless Reverse-Match Must Skip, Not Block On, a Rule Its Dropped Field Qualifies](conventions/a-stateless-reverse-match-must-skip-a-rule-its-dropped-field-qualifies.md) | Reconstructing "would the other side have accepted this?" from configuration alone (a stateful reverse-match, a reversed 5-tuple) where the reconstructed input has fewer fields than a rule can test; making a matcher ignore a field while a first-match loop breaks on the first match of any action; or reviewing two fixes to one predicate that pull opposite ways. |
| [A Search Dimension That Produces No Behavioral Variation Is Dishonest Coverage](conventions/a-search-dimension-with-no-behavioral-effect-is-dishonest-coverage.md) | Building a finite domain for a differential search, fuzzer, or parametric test whose size and coverage a caller reads as what was exercised; adding a dimension (a time, seed, mode, delay) whose effect passes through a downstream API you do not control; or judging whether enumerated tuples are behaviorally distinct or only nominally distinct. |
| [A Round Trip Through Your Own Codec Cannot Locate a Field on the Wire](conventions/a-codec-round-trip-cannot-locate-a-field-on-the-wire.md) | Adding or changing a packet encoder or decoder under `src/common/net/` or `src/common/netsim/`, including a new encapsulation over an existing frame; deciding what tests a wire format needs, or reviewing a codec whose tests are round trips and refusals; a simulated protocol that interoperates with itself while a real peer or a capture disagrees about a field. |
| [A Decoder That Accepts More Than Its Encoder Emits Loses Whatever You Queue and Re-Encode](architecture-patterns/a-decoder-wider-than-its-encoder-loses-whatever-you-queue.md) | Storing a decoded value and re-encoding it later — a hold queue, a retry buffer, a scheduler, a replay log — rather than encoding it while its caller can still be told; writing or reviewing a codec whose Decode and Encode do not validate the same set; a frame, message, or record that leaves a queue with no emission and no error. |
| [A Slot That Carries Two Roles Produces a Defect Class, and Each Reader Is a Separate Instance](architecture-patterns/one-slot-two-roles-is-a-defect-class-not-a-defect.md) | Reusing an identifier, slot, or zero value to mean a second thing so one field answers two questions by mode; reviewing a fix for a defect whose shape you have seen before in the same file; writing a classification table or reflection test meant to keep a structural rule true as a struct grows; deciding whether a lookup that cannot answer should fall back to a default. |
| [Comparing Two Forked Executions Attributes Each Observable by Provenance](architecture-patterns/comparing-two-forked-executions-attributes-observables-by-provenance.md) | Diffing two independently forked or aged executions of a simulator, replayer, or interpreter; deciding which produced items belong to the inputs under test versus shared history and how to pair them across sides; or finding a comparison over-reports differences from pre-existing state, or a comparison block never fires because its items are never in its input set. |
| [A Refusal Test Needs an Input Only the Refusal Rejects](conventions/a-refusal-test-needs-an-input-only-the-refusal-rejects.md) | Writing a test that asserts a function refuses, returning false, nil, or an error, where the function has more than one way to produce that outcome; choosing a test input by running the function under test until it returns the wanted result; reviewing a codec or validator whose tests are refusals, or judging whether a passing suite would notice a guard being deleted; adding a rule to a gate that already refuses, such as a second condition in a layering or validation table. |
| [A Contract That Must Never Reach a Refusal Is Tested By Enumerating the Refusals, Not the Reports That Reached One](conventions/a-degrade-contract-is-tested-against-the-refusing-side.md) | Writing or reviewing a translation boundary that promises to degrade bad input into recorded issues rather than fail; a second or third review round finds another input reaching a refusal the boundary was supposed to guard; deciding what test holds a contract phrased as a negative. |
| [A Merge of Two Feature Branches Is Green Exactly Where Neither Side's Fixtures Meet](architecture-patterns/a-merge-is-green-where-neither-sides-fixtures-meet.md) | Merging two branches that each added a feature to the same package, and deciding what the merged tree's green suite proves; resolving a conflict in a predicate one side narrowed and the other had copied elsewhere; planning the commits that follow a merge, and deciding which composition of the two features needs a test of its own; reviewing a merge commit, to decide whether a defect lives in the resolution or in what auto-merged around it. |
| [A Third Kind Joins a Two-Kind System Silently, So Enumerate the Sites Before Writing One](architecture-patterns/a-third-kind-joins-a-two-kind-system-silently.md) | Adding a node, endpoint, or participant kind to a package that holds exactly two and classifies by asking each one's map in turn; planning work whose stop condition is whether a new kind can be added without reshaping that classification; reviewing such a change to decide whether every site was found. |
| [An Untagged Frame's VLAN Is Local to Each End, So Two Ends' VLAN Ids Never Compare](architecture-patterns/an-untagged-frames-vlan-is-local-to-each-end.md) | Comparing a VLAN id seen at one point in a topology against one seen at another; deriving a finding from ingress classification on a port whose `Untagged` set may hold more than one VLAN; modelling 802.1Q egress membership or PVID behavior, or validating a switchport against what egress admits. |
| [A State Change a Transient Refusal Blocks Is a State Change Nothing Retries](architecture-patterns/state-a-transient-refusal-must-not-block-is-state-nothing-retries.md) | Making a record's delivery a precondition for the state it describes; reviewing a function that delivers several records around one state change where some retain a failure and others return on it; an operation resting in a non-terminal phase forever with the step that normally follows it never logged; a dedupe registry answering every re-dispatch as a duplicate for work that is not running. |
| [A Package Rename Moves Every Importer's Import Sort Key, and Reformatting Needs the Repo's Local Prefix](conventions/a-package-rename-moves-every-importers-sort-key.md) | Renaming or moving a protobuf package so every importer takes a new generated import path; a mechanical rename that builds and tests clean but fails golangci-lint on goimports in files you never opened; running gofumpt or goimports by hand over the files a mechanical change touched. |
| [A Protobuf Package Rename Breaks the Names You Persisted, Not the Records You Encoded](architecture-patterns/a-package-rename-breaks-names-you-persisted-not-records-you-encoded.md) | Renaming a protobuf package or message whose records are already written to a store, a queue, or a file; deciding whether such a rename needs a migration and what a pre-rename store does on the next start; writing the amendment that tells an operator what a rename costs; a service refusing to start against an existing store with `service/bus-migration-required`. |
| [Two Sources of "the Same" Metadata Can Use Different Vocabularies, and a Diff That Flags Everything Looks Like It Is Working](conventions/same-typed-metadata-maps-can-carry-different-vocabularies.md) | Comparing a locally recorded version, revision, or identifier against one a peer advertises, where a second transport or vendor can supply the same field; reading a drift or reconciliation report in which most entries differ, and deciding whether that is a finding about the peer or a broken comparison; adding a transport or vendor to a check that already works; reviewing two accessors documented as feeding one comparison where one is named for what it is compared against rather than what it returns. |
| [Holding a Secondary File Store to a Swept Record Requires In-Memory Ownership](architecture-patterns/holding-a-secondary-file-store-to-a-swept-record-requires-in-memory-ownership.md) | Pairing a metadata record store with a secondary filesystem payload store whose files are pruned by walking metadata records rather than the filesystem; writing an invariant test across dual stores, where an in-flight operation writes payload bytes before metadata finalization; or holding an invariant across operation sequences when multiple review rounds each patch an individual discrepancy between record state and disk state. |
| [A Schema Bound on a Streaming Payload Is Documentation Unless a Test Enforces It](conventions/a-schema-bound-on-a-streaming-payload-is-documentation-unless-a-test-enforces-it.md) | Declaring or changing a protovalidate constraint on a message carried on a streaming RPC, sizing batch constants or chunk buffers for streaming RPCs, or investigating a streaming message that violates schema rules without being rejected on arrival. |

Add a solution only after the behavior and lesson have been verified. Keep its
frontmatter specific enough that an agent can reject unrelated documents
without reading their full bodies.
