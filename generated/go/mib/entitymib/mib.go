// Code generated by mibgen; DO NOT EDIT.
//
// Source MIB:    ENTITY-MIB
// Source path:   spec/mib/ietf/ENTITY-MIB
// Source SHA-256: e56b77653a23171405dbae2431ed3f411dbe311e6ce1222cfd4cffd29dfe2e0c
//
// Regenerate with `go generate .` at the repository root.

package entitymib

import (
	"bytes"
	"context"
	"fmt"
	"iter"
	"time"

	snmp "go.aledante.io/FlowSeer/src/common/snmp"
	ae "go.aledante.io/ae"
)

// PhysicalClass is the SMI enum PhysicalClass.
// Starting with version 4 of the ENTITY-MIB, this TC is deprecated, and
// the usage of the IANAPhysicalClass TC from the IANA-ENTITY-MIB is
// recommended instead. An enumerated value that provides an indication of
// the general hardware type of a particular physical entity. There are no
// restrictions as to the number of entPhysicalEntries of each
// entPhysicalClass, which must be instantiated by an agent. The
// enumeration 'other' is applicable if the physical entity class is known
// but does not match any of the supported values. The enumeration
// 'unknown' is applicable if the physical entity class is unknown to the
// agent. The enumeration 'chassis' is applicable if the physical entity
// class is an overall container for networking equipment. Any class of
// physical entity, except a stack, may be contained within a chassis; a
// chassis may only be contained within a stack. The enumeration
// 'backplane' is applicable if the physical entity class is some sort of
// device for aggregating and forwarding networking traffic, such as a
// shared backplane in a modular ethernet switch. Note that an agent may
// model a backplane as a single physical entity, which is actually
// implemented as multiple discrete physical components (within a chassis
// or stack). The enumeration 'container' is applicable if the physical
// entity class is capable of containing one or more removable physical
// entities, possibly of different types. For example, each (empty or full)
// slot in a chassis will be modeled as a container. Note that all
// removable physical entities should be modeled within a container entity,
// such as field-replaceable modules, fans, or power supplies. Note that
// all known containers should be modeled by the agent, including empty
// containers. The enumeration 'powerSupply' is applicable if the physical
// entity class is a power-supplying component. The enumeration 'fan' is
// applicable if the physical entity class is a fan or other heat-reduction
// component. The enumeration 'sensor' is applicable if the physical entity
// class is some sort of sensor, such as a temperature sensor within a
// router chassis. The enumeration 'module' is applicable if the physical
// entity class is some sort of self-contained sub-system. If the
// enumeration 'module' is removable, then it should be modeled within a
// container entity; otherwise, it should be modeled directly within
// another physical entity (e.g., a chassis or another module). The
// enumeration 'port' is applicable if the physical entity class is some
// sort of networking port capable of receiving and/or transmitting
// networking traffic. The enumeration 'stack' is applicable if the
// physical entity class is some sort of super-container (possibly virtual)
// intended to group together multiple chassis entities. A stack may be
// realized by a 'virtual' cable, a real interconnect cable, attached to
// multiple chassis, or may in fact be comprised of multiple interconnect
// cables. A stack should not be modeled within any other physical
// entities, but a stack may be contained within another stack. Only
// chassis entities should be contained within a stack. The enumeration
// 'cpu' is applicable if the physical entity class is some sort of central
// processing unit.
type PhysicalClass int32

const (
	PhysicalClassOther       PhysicalClass = 1
	PhysicalClassUnknown     PhysicalClass = 2
	PhysicalClassChassis     PhysicalClass = 3
	PhysicalClassBackplane   PhysicalClass = 4
	PhysicalClassContainer   PhysicalClass = 5
	PhysicalClassPowerSupply PhysicalClass = 6
	PhysicalClassFan         PhysicalClass = 7
	PhysicalClassSensor      PhysicalClass = 8
	PhysicalClassModule      PhysicalClass = 9
	PhysicalClassPort        PhysicalClass = 10
	PhysicalClassStack       PhysicalClass = 11
	PhysicalClassCpu         PhysicalClass = 12
)

func (v PhysicalClass) String() string {
	switch v {
	case PhysicalClassOther:
		return "other"
	case PhysicalClassUnknown:
		return "unknown"
	case PhysicalClassChassis:
		return "chassis"
	case PhysicalClassBackplane:
		return "backplane"
	case PhysicalClassContainer:
		return "container"
	case PhysicalClassPowerSupply:
		return "powerSupply"
	case PhysicalClassFan:
		return "fan"
	case PhysicalClassSensor:
		return "sensor"
	case PhysicalClassModule:
		return "module"
	case PhysicalClassPort:
		return "port"
	case PhysicalClassStack:
		return "stack"
	case PhysicalClassCpu:
		return "cpu"
	}

	return fmt.Sprintf("PhysicalClass(%d)", v)
}

// EntLastChangeTimeGet reads the SMIv2 scalar entLastChangeTime.
// The value of sysUpTime at the time a conceptual row is created,
// modified, or deleted in any of these tables: - entPhysicalTable -
// entLogicalTable - entLPMappingTable - entAliasMappingTable -
// entPhysicalContainsTable
func EntLastChangeTimeGet(ctx context.Context, sess snmp.Session) (uint32, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 4, 1, 0)})
	if err != nil {
		return 0, err
	}

	if len(vbs) == 0 {
		return 0, ae.Msg("empty Get response for entLastChangeTime")
	}

	return func(vb snmp.VarBind) (uint32, error) {
		return snmp.DecodeUint32(vb)
	}(vbs[0])
}

// EntPhysicalDescr is the column entPhysicalDescr of table entPhysicalTable.
// A textual description of physical entity. This object should contain a
// string that identifies the manufacturer's name for the physical entity
// and should be set to a distinct value for each version or model of the
// physical entity.
var EntPhysicalDescr = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 2), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalVendorType is the column entPhysicalVendorType of table entPhysicalTable.
// An indication of the vendor-specific hardware type of the physical
// entity. Note that this is different from the definition of MIB-II's
// sysObjectID. An agent should set this object to an enterprise-specific
// registration identifier value indicating the specific equipment type in
// detail. The associated instance of entPhysicalClass is used to indicate
// the general type of hardware device. If no vendor-specific registration
// identifier exists for this physical entity, or the value is unknown by
// this agent, then the value { 0 0 } is returned.
var EntPhysicalVendorType = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 3), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// EntPhysicalContainedIn is the column entPhysicalContainedIn of table entPhysicalTable.
// The value of entPhysicalIndex for the physical entity that 'contains'
// this physical entity. A value of zero indicates this physical entity is
// not contained in any other physical entity. Note that the set of
// 'containment' relationships define a strict hierarchy; that is,
// recursion is not allowed. In the event that a physical entity is
// contained by more than one physical entity (e.g., double-wide modules),
// this object should identify the containing entity with the lowest value
// of entPhysicalIndex.
var EntPhysicalContainedIn = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 4), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// EntPhysicalClass is the column entPhysicalClass of table entPhysicalTable.
// An indication of the general hardware type of the physical entity. An
// agent should set this object to the standard enumeration value that most
// accurately indicates the general class of the physical entity, or the
// primary class if there is more than one entity. If no appropriate
// standard registration identifier exists for this physical entity, then
// the value 'other(1)' is returned. If the value is unknown by this agent,
// then the value 'unknown(2)' is returned.
var EntPhysicalClass = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 5), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// EntPhysicalParentRelPos is the column entPhysicalParentRelPos of table entPhysicalTable.
// An indication of the relative position of this 'child' component among
// all its 'sibling' components. Sibling components are defined as
// entPhysicalEntries that share the same instance values of each of the
// entPhysicalContainedIn and entPhysicalClass objects. An NMS can use this
// object to identify the relative ordering for all sibling components of a
// particular parent (identified by the entPhysicalContainedIn instance in
// each sibling entry). If possible, this value should match any external
// labeling of the physical component. For example, for a container (e.g.,
// card slot) labeled as 'slot #3', entPhysicalParentRelPos should have the
// value '3'. Note that the entPhysicalEntry for the module plugged in slot
// 3 should have an entPhysicalParentRelPos value of '1'. If the physical
// position of this component does not match any external numbering or
// clearly visible ordering, then user documentation or other external
// reference material should be used to determine the parent-relative
// position. If this is not possible, then the agent should assign a
// consistent (but possibly arbitrary) ordering to a given set of 'sibling'
// components, perhaps based on internal representation of the components.
// If the agent cannot determine the parent-relative position for some
// reason, or if the associated value of entPhysicalContainedIn is '0',
// then the value '-1' is returned. Otherwise, a non-negative integer is
// returned, indicating the parent-relative position of this physical
// entity. Parent-relative ordering normally starts from '1' and continues
// to 'N', where 'N' represents the highest positioned child entity.
// However, if the physical entities (e.g., slots) are labeled from a
// starting position of zero, then the first sibling should be associated
// with an entPhysicalParentRelPos value of '0'. Note that this ordering
// may be sparse or dense, depending on agent implementation. The actual
// values returned are not globally meaningful, as each 'parent' component
// may use different numbering algorithms. The ordering is only meaningful
// among siblings of the same parent component. The agent should retain
// parent-relative position values across reboots, either through
// algorithmic assignment or use of non-volatile storage.
var EntPhysicalParentRelPos = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 6), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// EntPhysicalName is the column entPhysicalName of table entPhysicalTable.
// The textual name of the physical entity. The value of this object should
// be the name of the component as assigned by the local device and should
// be suitable for use in commands entered at the device's 'console'. This
// might be a text name (e.g., 'console') or a simple component number
// (e.g., port or module number, such as '1'), depending on the physical
// component naming syntax of the device. If there is no local name, or if
// this object is otherwise not applicable, then this object contains a
// zero-length string. Note that the value of entPhysicalName for two
// physical entities will be the same in the event that the console
// interface does not distinguish between them, e.g., slot-1 and the card
// in slot-1.
var EntPhysicalName = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 7), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalHardwareRev is the column entPhysicalHardwareRev of table entPhysicalTable.
// The vendor-specific hardware revision string for the physical entity.
// The preferred value is the hardware revision identifier actually printed
// on the component itself (if present). Note that if revision information
// is stored internally in a non-printable (e.g., binary) format, then the
// agent must convert such information to a printable format in an
// implementation-specific manner. If no specific hardware revision string
// is associated with the physical component, or if this information is
// unknown to the agent, then this object will contain a zero-length
// string.
var EntPhysicalHardwareRev = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 8), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalFirmwareRev is the column entPhysicalFirmwareRev of table entPhysicalTable.
// The vendor-specific firmware revision string for the physical entity.
// Note that if revision information is stored internally in a
// non-printable (e.g., binary) format, then the agent must convert such
// information to a printable format in an implementation-specific manner.
// If no specific firmware programs are associated with the physical
// component, or if this information is unknown to the agent, then this
// object will contain a zero-length string.
var EntPhysicalFirmwareRev = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 9), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalSoftwareRev is the column entPhysicalSoftwareRev of table entPhysicalTable.
// The vendor-specific software revision string for the physical entity.
// Note that if revision information is stored internally in a
// non-printable (e.g., binary) format, then the agent must convert such
// information to a printable format in an implementation-specific manner.
// If no specific software programs are associated with the physical
// component, or if this information is unknown to the agent, then this
// object will contain a zero-length string.
var EntPhysicalSoftwareRev = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 10), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalSerialNum is the column entPhysicalSerialNum of table entPhysicalTable.
// The vendor-specific serial number string for the physical entity. The
// preferred value is the serial number string actually printed on the
// component itself (if present). On the first instantiation of a physical
// entity, the value of entPhysicalSerialNum associated with that entity is
// set to the correct vendor-assigned serial number, if this information is
// available to the agent. If a serial number is unknown or non-existent,
// the entPhysicalSerialNum will be set to a zero-length string instead.
// Note that implementations that can correctly identify the serial numbers
// of all installed physical entities do not need to provide write access
// to the entPhysicalSerialNum object. Agents that cannot provide
// non-volatile storage for the entPhysicalSerialNum strings are not
// required to implement write access for this object. Not every physical
// component will have a serial number, or even need one. Physical entities
// for which the associated value of the entPhysicalIsFRU object is equal
// to 'false(2)' (e.g., the repeater ports within a repeater module) do not
// need their own unique serial numbers. An agent does not have to provide
// write access for such entities and may return a zero-length string. If
// write access is implemented for an instance of entPhysicalSerialNum and
// a value is written into the instance, the agent must retain the supplied
// value in the entPhysicalSerialNum instance (associated with the same
// physical entity) for as long as that entity remains instantiated. This
// includes instantiations across all re-initializations/reboots of the
// network management system, including those resulting in a change of the
// physical entity's entPhysicalIndex value.
var EntPhysicalSerialNum = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 11), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalMfgName is the column entPhysicalMfgName of table entPhysicalTable.
// The name of the manufacturer of this physical component. The preferred
// value is the manufacturer name string actually printed on the component
// itself (if present). Note that comparisons between instances of the
// entPhysicalModelName, entPhysicalFirmwareRev, entPhysicalSoftwareRev,
// and the entPhysicalSerialNum objects are only meaningful amongst
// entPhysicalEntries with the same value of entPhysicalMfgName. If the
// manufacturer name string associated with the physical component is
// unknown to the agent, then this object will contain a zero-length
// string.
var EntPhysicalMfgName = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 12), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalModelName is the column entPhysicalModelName of table entPhysicalTable.
// The vendor-specific model name identifier string associated with this
// physical component. The preferred value is the customer-visible part
// number, which may be printed on the component itself. If the model name
// string associated with the physical component is unknown to the agent,
// then this object will contain a zero-length string.
var EntPhysicalModelName = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 13), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalAlias is the column entPhysicalAlias of table entPhysicalTable.
// This object is an 'alias' name for the physical entity, as specified by
// a network manager, and provides a non-volatile 'handle' for the physical
// entity. On the first instantiation of a physical entity, the value of
// entPhysicalAlias associated with that entity is set to the zero-length
// string. However, the agent may set the value to a locally unique default
// value, instead of a zero-length string. If write access is implemented
// for an instance of entPhysicalAlias and a value is written into the
// instance, the agent must retain the supplied value in the
// entPhysicalAlias instance (associated with the same physical entity) for
// as long as that entity remains instantiated. This includes
// instantiations across all re-initializations/reboots of the network
// management system, including those resulting in a change of the physical
// entity's entPhysicalIndex value.
var EntPhysicalAlias = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 14), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalAssetID is the column entPhysicalAssetID of table entPhysicalTable.
// This object is a user-assigned asset tracking identifier (as specified
// by a network manager) for the physical entity and provides non-volatile
// storage of this information. On the first instantiation of a physical
// entity, the value of entPhysicalAssetID associated with that entity is
// set to the zero-length string. Not every physical component will have an
// asset tracking identifier or even need one. Physical entities for which
// the associated value of the entPhysicalIsFRU object is equal to
// 'false(2)' (e.g., the repeater ports within a repeater module) do not
// need their own unique asset tracking identifier. An agent does not have
// to provide write access for such entities and may instead return a
// zero-length string. If write access is implemented for an instance of
// entPhysicalAssetID and a value is written into the instance, the agent
// must retain the supplied value in the entPhysicalAssetID instance
// (associated with the same physical entity) for as long as that entity
// remains instantiated. This includes instantiations across all
// re-initializations/reboots of the network management system, including
// those resulting in a change of the physical entity's entPhysicalIndex
// value. If no asset tracking information is associated with the physical
// component, then this object will contain a zero-length string.
var EntPhysicalAssetID = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 15), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalIsFRU is the column entPhysicalIsFRU of table entPhysicalTable.
// This object indicates whether or not this physical entity is considered
// a 'field replaceable unit' by the vendor. If this object contains the
// value 'true(1)', then this entPhysicalEntry identifies a field
// replaceable unit. For all entPhysicalEntries that represent components
// permanently contained within a field replaceable unit, the value
// 'false(2)' should be returned for this object.
var EntPhysicalIsFRU = snmp.NewColumn[bool](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 16), snmp.KindInteger32, func(vb snmp.VarBind) (bool, error) {
	return snmp.DecodeTruthValue(vb)
})

// EntPhysicalMfgDate is the column entPhysicalMfgDate of table entPhysicalTable.
// This object contains the date of manufacturing of the managed entity. If
// the manufacturing date is unknown or not supported, the object is not
// instantiated. The special value '0000000000000000'H may also be returned
// in this case.
var EntPhysicalMfgDate = snmp.NewColumn[time.Time](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 17), snmp.KindOctetString, func(vb snmp.VarBind) (time.Time, error) {
	return snmp.DecodeDateAndTime(vb)
})

// EntPhysicalUris is the column entPhysicalUris of table entPhysicalTable.
// This object contains identification information about the physical
// entity. The object contains URIs; therefore, the syntax of this object
// must conform to RFC 3986, Section 3. Multiple URIs may be present and
// are separated by white space characters. Leading and trailing white
// space characters are ignored. If no URI identification information is
// known about the physical entity, the object is not instantiated. A
// zero-length octet string may also be returned in this case.
var EntPhysicalUris = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 18), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalUUID is the column entPhysicalUUID of table entPhysicalTable.
// This object contains identification information about the physical
// entity. The object contains a Universally Unique Identifier, the syntax
// of this object must conform to RFC 4122, Section 4.1. A zero-length
// octet string is returned if no UUID information is known.
var EntPhysicalUUID = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 19), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntPhysicalTableRow is one row of entPhysicalTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk().
type EntPhysicalTableRow struct {
	Index                   snmp.OID
	EntPhysicalDescr        []byte
	EntPhysicalVendorType   snmp.OID
	EntPhysicalContainedIn  int32
	EntPhysicalClass        int32
	EntPhysicalParentRelPos int32
	EntPhysicalName         []byte
	EntPhysicalHardwareRev  []byte
	EntPhysicalFirmwareRev  []byte
	EntPhysicalSoftwareRev  []byte
	EntPhysicalSerialNum    []byte
	EntPhysicalMfgName      []byte
	EntPhysicalModelName    []byte
	EntPhysicalAlias        []byte
	EntPhysicalAssetID      []byte
	EntPhysicalIsFRU        bool
	EntPhysicalMfgDate      time.Time
	EntPhysicalUris         []byte
	EntPhysicalUUID         []byte
}

// EntPhysicalTableWalker is a table-aware walker over entPhysicalTable.
// Construct via EntPhysicalTable.Walk(ctx, sess, cols...).
type EntPhysicalTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *EntPhysicalTableWalker) Iter() iter.Seq2[snmp.OID, EntPhysicalTableRow] {
	return func(yield func(snmp.OID, EntPhysicalTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1).WireBytes()
		buffer := make(map[string]*EntPhysicalTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &EntPhysicalTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 2:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalDescr.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalDescr = dv
					}
				}
			case 3:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalVendorType.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalVendorType = dv
					}
				}
			case 4:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.EntPhysicalContainedIn = int32(v)
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := EntPhysicalContainedIn.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.EntPhysicalContainedIn = dv
						}
					}
				}
			case 5:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.EntPhysicalClass = int32(v)
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := EntPhysicalClass.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.EntPhysicalClass = dv
						}
					}
				}
			case 6:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.EntPhysicalParentRelPos = int32(v)
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := EntPhysicalParentRelPos.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.EntPhysicalParentRelPos = dv
						}
					}
				}
			case 7:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalName.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalName = dv
					}
				}
			case 8:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalHardwareRev.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalHardwareRev = dv
					}
				}
			case 9:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalFirmwareRev.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalFirmwareRev = dv
					}
				}
			case 10:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalSoftwareRev.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalSoftwareRev = dv
					}
				}
			case 11:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalSerialNum.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalSerialNum = dv
					}
				}
			case 12:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalMfgName.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalMfgName = dv
					}
				}
			case 13:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalModelName.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalModelName = dv
					}
				}
			case 14:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalAlias.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalAlias = dv
					}
				}
			case 15:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalAssetID.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalAssetID = dv
					}
				}
			case 16:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalIsFRU.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalIsFRU = dv
					}
				}
			case 17:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalMfgDate.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalMfgDate = dv
					}
				}
			case 18:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalUris.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalUris = dv
					}
				}
			case 19:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntPhysicalUUID.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntPhysicalUUID = dv
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *EntPhysicalTableWalker) Err() error {
	return tw.rw.Err()
}

// entPhysicalTableT is the singleton type of EntPhysicalTable.
type entPhysicalTableT struct{}

// EntPhysicalTable is the descriptor for the entPhysicalTable table.
var EntPhysicalTable entPhysicalTableT

// Walk launches a BulkWalk over entPhysicalTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
func (entPhysicalTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *EntPhysicalTableWalker {
	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1))
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() == 0 {
			continue
		}
		byCol[o.At(o.Len()-1)] = c
	}

	return &EntPhysicalTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// decodeEntPhysicalTableRow decodes one row of EntPhysicalTable from the supplied
// VarBinds. Each VarBind's OID determines which row field it populates
// (via the column's last sub-id). VarBinds with unknown column-ids are
// ignored. Absent columns leave their field at its zero value.
func decodeEntPhysicalTableRow(idx snmp.OID, vbs []snmp.VarBind) (EntPhysicalTableRow, error) {
	var row EntPhysicalTableRow
	row.Index = idx

	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 2:
			dv, derr := EntPhysicalDescr.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalDescr = dv
		case 3:
			dv, derr := EntPhysicalVendorType.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalVendorType = dv
		case 4:
			dv, derr := EntPhysicalContainedIn.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalContainedIn = dv
		case 5:
			dv, derr := EntPhysicalClass.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalClass = dv
		case 6:
			dv, derr := EntPhysicalParentRelPos.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalParentRelPos = dv
		case 7:
			dv, derr := EntPhysicalName.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalName = dv
		case 8:
			dv, derr := EntPhysicalHardwareRev.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalHardwareRev = dv
		case 9:
			dv, derr := EntPhysicalFirmwareRev.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalFirmwareRev = dv
		case 10:
			dv, derr := EntPhysicalSoftwareRev.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalSoftwareRev = dv
		case 11:
			dv, derr := EntPhysicalSerialNum.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalSerialNum = dv
		case 12:
			dv, derr := EntPhysicalMfgName.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalMfgName = dv
		case 13:
			dv, derr := EntPhysicalModelName.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalModelName = dv
		case 14:
			dv, derr := EntPhysicalAlias.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalAlias = dv
		case 15:
			dv, derr := EntPhysicalAssetID.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalAssetID = dv
		case 16:
			dv, derr := EntPhysicalIsFRU.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalIsFRU = dv
		case 17:
			dv, derr := EntPhysicalMfgDate.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalMfgDate = dv
		case 18:
			dv, derr := EntPhysicalUris.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalUris = dv
		case 19:
			dv, derr := EntPhysicalUUID.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalUUID = dv
		}
	}

	return row, nil
}

// equalEntPhysicalTableRow compares two EntPhysicalTableRow values for equality.
// Used by [snmp.Watcher] to compute ChangeKindModified emits. Field-by-
// field with the type-appropriate comparator (bytes.Equal for []byte,
// OID.Equal for OID, time.Time.Equal for time.Time, == for everything else).
func equalEntPhysicalTableRow(a EntPhysicalTableRow, b EntPhysicalTableRow) bool {
	return a.Index.Equal(b.Index) && bytes.Equal(a.EntPhysicalDescr, b.EntPhysicalDescr) && a.EntPhysicalVendorType.Equal(b.EntPhysicalVendorType) && a.EntPhysicalContainedIn == b.EntPhysicalContainedIn && a.EntPhysicalClass == b.EntPhysicalClass && a.EntPhysicalParentRelPos == b.EntPhysicalParentRelPos && bytes.Equal(a.EntPhysicalName, b.EntPhysicalName) && bytes.Equal(a.EntPhysicalHardwareRev, b.EntPhysicalHardwareRev) && bytes.Equal(a.EntPhysicalFirmwareRev, b.EntPhysicalFirmwareRev) && bytes.Equal(a.EntPhysicalSoftwareRev, b.EntPhysicalSoftwareRev) && bytes.Equal(a.EntPhysicalSerialNum, b.EntPhysicalSerialNum) && bytes.Equal(a.EntPhysicalMfgName, b.EntPhysicalMfgName) && bytes.Equal(a.EntPhysicalModelName, b.EntPhysicalModelName) && bytes.Equal(a.EntPhysicalAlias, b.EntPhysicalAlias) && bytes.Equal(a.EntPhysicalAssetID, b.EntPhysicalAssetID) && a.EntPhysicalIsFRU == b.EntPhysicalIsFRU && a.EntPhysicalMfgDate.Equal(b.EntPhysicalMfgDate) && bytes.Equal(a.EntPhysicalUris, b.EntPhysicalUris) && bytes.Equal(a.EntPhysicalUUID, b.EntPhysicalUUID)
}

// mergeEntPhysicalTableRow merges the values decoded from vbs into dst, leaving fields
// whose columns are not present in vbs unchanged. Used by [snmp.Watcher]
// to maintain per-row state under partial-column fetches (Counter-tier,
// Static-tier). Best-effort: individual VarBind decode failures are
// silently skipped rather than propagated, because partial-fetch ticks
// surface transient errors through [snmp.Watcher.LastTickErr] at the
// call-site granularity rather than per-VarBind.
func mergeEntPhysicalTableRow(dst *EntPhysicalTableRow, vbs []snmp.VarBind) {
	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 2:
			dv, derr := EntPhysicalDescr.Decode(vb)
			if derr == nil {
				dst.EntPhysicalDescr = dv
			}
		case 3:
			dv, derr := EntPhysicalVendorType.Decode(vb)
			if derr == nil {
				dst.EntPhysicalVendorType = dv
			}
		case 4:
			dv, derr := EntPhysicalContainedIn.Decode(vb)
			if derr == nil {
				dst.EntPhysicalContainedIn = dv
			}
		case 5:
			dv, derr := EntPhysicalClass.Decode(vb)
			if derr == nil {
				dst.EntPhysicalClass = dv
			}
		case 6:
			dv, derr := EntPhysicalParentRelPos.Decode(vb)
			if derr == nil {
				dst.EntPhysicalParentRelPos = dv
			}
		case 7:
			dv, derr := EntPhysicalName.Decode(vb)
			if derr == nil {
				dst.EntPhysicalName = dv
			}
		case 8:
			dv, derr := EntPhysicalHardwareRev.Decode(vb)
			if derr == nil {
				dst.EntPhysicalHardwareRev = dv
			}
		case 9:
			dv, derr := EntPhysicalFirmwareRev.Decode(vb)
			if derr == nil {
				dst.EntPhysicalFirmwareRev = dv
			}
		case 10:
			dv, derr := EntPhysicalSoftwareRev.Decode(vb)
			if derr == nil {
				dst.EntPhysicalSoftwareRev = dv
			}
		case 11:
			dv, derr := EntPhysicalSerialNum.Decode(vb)
			if derr == nil {
				dst.EntPhysicalSerialNum = dv
			}
		case 12:
			dv, derr := EntPhysicalMfgName.Decode(vb)
			if derr == nil {
				dst.EntPhysicalMfgName = dv
			}
		case 13:
			dv, derr := EntPhysicalModelName.Decode(vb)
			if derr == nil {
				dst.EntPhysicalModelName = dv
			}
		case 14:
			dv, derr := EntPhysicalAlias.Decode(vb)
			if derr == nil {
				dst.EntPhysicalAlias = dv
			}
		case 15:
			dv, derr := EntPhysicalAssetID.Decode(vb)
			if derr == nil {
				dst.EntPhysicalAssetID = dv
			}
		case 16:
			dv, derr := EntPhysicalIsFRU.Decode(vb)
			if derr == nil {
				dst.EntPhysicalIsFRU = dv
			}
		case 17:
			dv, derr := EntPhysicalMfgDate.Decode(vb)
			if derr == nil {
				dst.EntPhysicalMfgDate = dv
			}
		case 18:
			dv, derr := EntPhysicalUris.Decode(vb)
			if derr == nil {
				dst.EntPhysicalUris = dv
			}
		case 19:
			dv, derr := EntPhysicalUUID.Decode(vb)
			if derr == nil {
				dst.EntPhysicalUUID = dv
			}
		}
	}
}

// EntPhysicalTableWatcher is a table-aware Watcher over EntPhysicalTable.
// Construct via EntPhysicalTable.Watch(ctx, sess, cols, opts...).
type EntPhysicalTableWatcher struct {
	w *snmp.Watcher[EntPhysicalTableRow]
}

// Iter returns the range-over-func view of the Watcher's event stream.
// See [snmp.Watcher.Iter] for the contract.
func (tw *EntPhysicalTableWatcher) Iter() iter.Seq2[snmp.OID, snmp.WatchEvent[EntPhysicalTableRow]] {
	return tw.w.Iter()
}

// Err returns the underlying Watcher's terminal error, or nil if it
// completed naturally. See [snmp.Watcher.Err].
func (tw *EntPhysicalTableWatcher) Err() error {
	return tw.w.Err()
}

// Close signals the Watcher's tick goroutine to terminate. Idempotent.
// See [snmp.Watcher.Close].
func (tw *EntPhysicalTableWatcher) Close() error {
	return tw.w.Close()
}

// Fallback reports whether the Watcher has transitioned to fallback
// mode. See [snmp.Watcher.Fallback].
func (tw *EntPhysicalTableWatcher) Fallback() bool {
	return tw.w.Fallback()
}

// LastTickErr returns the most-recent transient per-tick error.
// See [snmp.Watcher.LastTickErr].
func (tw *EntPhysicalTableWatcher) LastTickErr() error {
	return tw.w.LastTickErr()
}

// TableRoot returns the OID of the table this Watcher operates over.
func (tw *EntPhysicalTableWatcher) TableRoot() snmp.OID {
	return tw.w.TableRoot()
}

// Watch opens a long-lived watch on EntPhysicalTable.
//
// cols selects the columns whose values are reported on every emitted
// event. opts override cadence bounds, per-column tier classifications,
// fallback behavior, and other policy — see [snmp.WatchOption] for the
// full set.
//
// cols is a slice rather than variadic because opts is variadic and
// Go forbids two variadic parameters.
//
// Events emitted on this stream:
//   - ChangeKindAdded    — Row populated, Prev always nil.
//   - ChangeKindModified — Row is current state. Prev is nil unless
//     the caller passed [snmp.WithPrevRow]; when
//     set, Prev carries the previous Row.
//   - ChangeKindRemoved  — Row carries the last-known state at the
//     time the row disappeared. Prev is nil.
//
// The returned Watcher must be Closed when the caller is done; iteration
// exit alone does not free the underlying goroutine until Close.
//
// On validation failure (invalid cadence bounds, tier override of the
// indicator column, etc.) Watch returns a Watcher whose Err() returns
// the cause immediately; range loops exit without emitting events and
// Close is a no-op.
func (entPhysicalTableT) Watch(ctx context.Context, sess snmp.Session, cols []snmp.AnyColumn, opts ...snmp.WatchOption) *EntPhysicalTableWatcher {
	allOpts := append([]snmp.WatchOption{snmp.WithTierLookup(ColumnTier)}, opts...)
	w, _ := snmp.NewWatcher[EntPhysicalTableRow](ctx, sess, EntPhysicalTableIndicator, cols, decodeEntPhysicalTableRow, equalEntPhysicalTableRow, mergeEntPhysicalTableRow, allOpts...)

	return &EntPhysicalTableWatcher{w: w}
}

// EntLogicalDescr is the column entLogicalDescr of table entLogicalTable.
// A textual description of the logical entity. This object should contain
// a string that identifies the manufacturer's name for the logical entity
// and should be set to a distinct value for each version of the logical
// entity.
var EntLogicalDescr = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 2), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntLogicalType is the column entLogicalType of table entLogicalTable.
// An indication of the type of logical entity. This will typically be the
// OBJECT IDENTIFIER name of the node in the SMI's naming hierarchy that
// represents the major MIB module, or the majority of the MIB modules,
// supported by the logical entity. For example: a logical entity of a
// regular host/router -> mib-2 a logical entity of a 802.1d bridge ->
// dot1dBridge a logical entity of a 802.3 repeater -> snmpDot3RptrMgmt If
// an appropriate node in the SMI's naming hierarchy cannot be identified,
// the value 'mib-2' should be used.
var EntLogicalType = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 3), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// EntLogicalCommunity is the column entLogicalCommunity of table entLogicalTable.
// An SNMPv1 or SNMPv2c community string, which can be used to access
// detailed management information for this logical entity. The agent
// should allow read access with this community string (to an appropriate
// subset of all managed objects) and may also return a community string
// based on the privileges of the request used to read this object. Note
// that an agent may return a community string with read-only privileges,
// even if this object is accessed with a read-write community string.
// However, the agent must take care not to return a community string that
// allows more privileges than the community string used to access this
// object. A compliant SNMP agent may wish to conserve naming scopes by
// representing multiple logical entities in a single 'default' naming
// scope. This is possible when the logical entities, represented by the
// same value of entLogicalCommunity, have no object instances in common.
// For example, 'bridge1' and 'repeater1' may be part of the main naming
// scope, but at least one additional community string is needed to
// represent 'bridge2' and 'repeater2'. Logical entities 'bridge1' and
// 'repeater1' would be represented by sysOREntries associated with the
// 'default' naming scope. For agents not accessible via SNMPv1 or SNMPv2c,
// the value of this object is the empty string. This object may also
// contain an empty string if a community string has not yet been assigned
// by the agent or if no community string with suitable access rights can
// be returned for a particular SNMP request. Note that this object is
// deprecated. Agents that implement SNMPv3 access should use the
// entLogicalContextEngineID and entLogicalContextName objects to identify
// the context associated with each logical entity. SNMPv3 agents may
// return a zero-length string for this object or may continue to return a
// community string (e.g., tri-lingual agent support).
var EntLogicalCommunity = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 4), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntLogicalTAddress is the column entLogicalTAddress of table entLogicalTable.
// The transport service address by which the logical entity receives
// network management traffic, formatted according to the corresponding
// value of entLogicalTDomain. For snmpUDPDomain, a TAddress is 6 octets
// long: the initial 4 octets contain the IP-address in network-byte order,
// and the last 2 contain the UDP port in network-byte order. Consult RFC
// 3417 for further information on snmpUDPDomain.
var EntLogicalTAddress = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 5), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntLogicalTDomain is the column entLogicalTDomain of table entLogicalTable.
// Indicates the kind of transport service by which the logical entity
// receives network management traffic. Possible values for this object are
// presently found in RFC 3417.
var EntLogicalTDomain = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 6), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// EntLogicalContextEngineID is the column entLogicalContextEngineID of table entLogicalTable.
// The authoritative contextEngineID that can be used to send an SNMP
// message concerning information held by this logical entity to the
// address specified by the associated
// 'entLogicalTAddress/entLogicalTDomain' pair. This object, together with
// the associated entLogicalContextName object, defines the context
// associated with a particular logical entity and allows access to SNMP
// engines identified by a contextEngineID and contextName pair. If no
// value has been configured by the agent, a zero-length string is
// returned, or the agent may choose not to instantiate this object at all.
var EntLogicalContextEngineID = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 7), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntLogicalContextName is the column entLogicalContextName of table entLogicalTable.
// The contextName that can be used to send an SNMP message concerning
// information held by this logical entity to the address specified by the
// associated 'entLogicalTAddress/entLogicalTDomain' pair. This object,
// together with the associated entLogicalContextEngineID object, defines
// the context associated with a particular logical entity and allows
// access to SNMP engines identified by a contextEngineID and contextName
// pair. If no value has been configured by the agent, a zero-length string
// is returned, or the agent may choose not to instantiate this object at
// all.
var EntLogicalContextName = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 8), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// EntLogicalTableRow is one row of entLogicalTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk().
type EntLogicalTableRow struct {
	Index                     snmp.OID
	EntLogicalDescr           []byte
	EntLogicalType            snmp.OID
	EntLogicalCommunity       []byte
	EntLogicalTAddress        []byte
	EntLogicalTDomain         snmp.OID
	EntLogicalContextEngineID []byte
	EntLogicalContextName     []byte
}

// EntLogicalTableWalker is a table-aware walker over entLogicalTable.
// Construct via EntLogicalTable.Walk(ctx, sess, cols...).
type EntLogicalTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *EntLogicalTableWalker) Iter() iter.Seq2[snmp.OID, EntLogicalTableRow] {
	return func(yield func(snmp.OID, EntLogicalTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1).WireBytes()
		buffer := make(map[string]*EntLogicalTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &EntLogicalTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 2:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntLogicalDescr.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntLogicalDescr = dv
					}
				}
			case 3:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntLogicalType.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntLogicalType = dv
					}
				}
			case 4:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntLogicalCommunity.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntLogicalCommunity = dv
					}
				}
			case 5:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntLogicalTAddress.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntLogicalTAddress = dv
					}
				}
			case 6:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntLogicalTDomain.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntLogicalTDomain = dv
					}
				}
			case 7:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntLogicalContextEngineID.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntLogicalContextEngineID = dv
					}
				}
			case 8:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntLogicalContextName.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntLogicalContextName = dv
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *EntLogicalTableWalker) Err() error {
	return tw.rw.Err()
}

// entLogicalTableT is the singleton type of EntLogicalTable.
type entLogicalTableT struct{}

// EntLogicalTable is the descriptor for the entLogicalTable table.
var EntLogicalTable entLogicalTableT

// Walk launches a BulkWalk over entLogicalTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
func (entLogicalTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *EntLogicalTableWalker {
	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1))
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() == 0 {
			continue
		}
		byCol[o.At(o.Len()-1)] = c
	}

	return &EntLogicalTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// decodeEntLogicalTableRow decodes one row of EntLogicalTable from the supplied
// VarBinds. Each VarBind's OID determines which row field it populates
// (via the column's last sub-id). VarBinds with unknown column-ids are
// ignored. Absent columns leave their field at its zero value.
func decodeEntLogicalTableRow(idx snmp.OID, vbs []snmp.VarBind) (EntLogicalTableRow, error) {
	var row EntLogicalTableRow
	row.Index = idx

	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 2:
			dv, derr := EntLogicalDescr.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntLogicalDescr = dv
		case 3:
			dv, derr := EntLogicalType.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntLogicalType = dv
		case 4:
			dv, derr := EntLogicalCommunity.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntLogicalCommunity = dv
		case 5:
			dv, derr := EntLogicalTAddress.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntLogicalTAddress = dv
		case 6:
			dv, derr := EntLogicalTDomain.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntLogicalTDomain = dv
		case 7:
			dv, derr := EntLogicalContextEngineID.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntLogicalContextEngineID = dv
		case 8:
			dv, derr := EntLogicalContextName.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntLogicalContextName = dv
		}
	}

	return row, nil
}

// equalEntLogicalTableRow compares two EntLogicalTableRow values for equality.
// Used by [snmp.Watcher] to compute ChangeKindModified emits. Field-by-
// field with the type-appropriate comparator (bytes.Equal for []byte,
// OID.Equal for OID, time.Time.Equal for time.Time, == for everything else).
func equalEntLogicalTableRow(a EntLogicalTableRow, b EntLogicalTableRow) bool {
	return a.Index.Equal(b.Index) && bytes.Equal(a.EntLogicalDescr, b.EntLogicalDescr) && a.EntLogicalType.Equal(b.EntLogicalType) && bytes.Equal(a.EntLogicalCommunity, b.EntLogicalCommunity) && bytes.Equal(a.EntLogicalTAddress, b.EntLogicalTAddress) && a.EntLogicalTDomain.Equal(b.EntLogicalTDomain) && bytes.Equal(a.EntLogicalContextEngineID, b.EntLogicalContextEngineID) && bytes.Equal(a.EntLogicalContextName, b.EntLogicalContextName)
}

// mergeEntLogicalTableRow merges the values decoded from vbs into dst, leaving fields
// whose columns are not present in vbs unchanged. Used by [snmp.Watcher]
// to maintain per-row state under partial-column fetches (Counter-tier,
// Static-tier). Best-effort: individual VarBind decode failures are
// silently skipped rather than propagated, because partial-fetch ticks
// surface transient errors through [snmp.Watcher.LastTickErr] at the
// call-site granularity rather than per-VarBind.
func mergeEntLogicalTableRow(dst *EntLogicalTableRow, vbs []snmp.VarBind) {
	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 2:
			dv, derr := EntLogicalDescr.Decode(vb)
			if derr == nil {
				dst.EntLogicalDescr = dv
			}
		case 3:
			dv, derr := EntLogicalType.Decode(vb)
			if derr == nil {
				dst.EntLogicalType = dv
			}
		case 4:
			dv, derr := EntLogicalCommunity.Decode(vb)
			if derr == nil {
				dst.EntLogicalCommunity = dv
			}
		case 5:
			dv, derr := EntLogicalTAddress.Decode(vb)
			if derr == nil {
				dst.EntLogicalTAddress = dv
			}
		case 6:
			dv, derr := EntLogicalTDomain.Decode(vb)
			if derr == nil {
				dst.EntLogicalTDomain = dv
			}
		case 7:
			dv, derr := EntLogicalContextEngineID.Decode(vb)
			if derr == nil {
				dst.EntLogicalContextEngineID = dv
			}
		case 8:
			dv, derr := EntLogicalContextName.Decode(vb)
			if derr == nil {
				dst.EntLogicalContextName = dv
			}
		}
	}
}

// EntLogicalTableWatcher is a table-aware Watcher over EntLogicalTable.
// Construct via EntLogicalTable.Watch(ctx, sess, cols, opts...).
type EntLogicalTableWatcher struct {
	w *snmp.Watcher[EntLogicalTableRow]
}

// Iter returns the range-over-func view of the Watcher's event stream.
// See [snmp.Watcher.Iter] for the contract.
func (tw *EntLogicalTableWatcher) Iter() iter.Seq2[snmp.OID, snmp.WatchEvent[EntLogicalTableRow]] {
	return tw.w.Iter()
}

// Err returns the underlying Watcher's terminal error, or nil if it
// completed naturally. See [snmp.Watcher.Err].
func (tw *EntLogicalTableWatcher) Err() error {
	return tw.w.Err()
}

// Close signals the Watcher's tick goroutine to terminate. Idempotent.
// See [snmp.Watcher.Close].
func (tw *EntLogicalTableWatcher) Close() error {
	return tw.w.Close()
}

// Fallback reports whether the Watcher has transitioned to fallback
// mode. See [snmp.Watcher.Fallback].
func (tw *EntLogicalTableWatcher) Fallback() bool {
	return tw.w.Fallback()
}

// LastTickErr returns the most-recent transient per-tick error.
// See [snmp.Watcher.LastTickErr].
func (tw *EntLogicalTableWatcher) LastTickErr() error {
	return tw.w.LastTickErr()
}

// TableRoot returns the OID of the table this Watcher operates over.
func (tw *EntLogicalTableWatcher) TableRoot() snmp.OID {
	return tw.w.TableRoot()
}

// Watch opens a long-lived watch on EntLogicalTable.
//
// cols selects the columns whose values are reported on every emitted
// event. opts override cadence bounds, per-column tier classifications,
// fallback behavior, and other policy — see [snmp.WatchOption] for the
// full set.
//
// cols is a slice rather than variadic because opts is variadic and
// Go forbids two variadic parameters.
//
// Events emitted on this stream:
//   - ChangeKindAdded    — Row populated, Prev always nil.
//   - ChangeKindModified — Row is current state. Prev is nil unless
//     the caller passed [snmp.WithPrevRow]; when
//     set, Prev carries the previous Row.
//   - ChangeKindRemoved  — Row carries the last-known state at the
//     time the row disappeared. Prev is nil.
//
// The returned Watcher must be Closed when the caller is done; iteration
// exit alone does not free the underlying goroutine until Close.
//
// On validation failure (invalid cadence bounds, tier override of the
// indicator column, etc.) Watch returns a Watcher whose Err() returns
// the cause immediately; range loops exit without emitting events and
// Close is a no-op.
func (entLogicalTableT) Watch(ctx context.Context, sess snmp.Session, cols []snmp.AnyColumn, opts ...snmp.WatchOption) *EntLogicalTableWatcher {
	allOpts := append([]snmp.WatchOption{snmp.WithTierLookup(ColumnTier)}, opts...)
	w, _ := snmp.NewWatcher[EntLogicalTableRow](ctx, sess, EntLogicalTableIndicator, cols, decodeEntLogicalTableRow, equalEntLogicalTableRow, mergeEntLogicalTableRow, allOpts...)

	return &EntLogicalTableWatcher{w: w}
}

// EntLPPhysicalIndex is the column entLPPhysicalIndex of table entLPMappingTable.
// The value of this object identifies the index value of a particular
// entPhysicalEntry associated with the indicated entLogicalEntity.
var EntLPPhysicalIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 1, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// EntLPMappingTableRow is one row of entLPMappingTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk().
type EntLPMappingTableRow struct {
	Index              snmp.OID
	EntLPPhysicalIndex int32
}

// EntLPMappingTableWalker is a table-aware walker over entLPMappingTable.
// Construct via EntLPMappingTable.Walk(ctx, sess, cols...).
type EntLPMappingTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *EntLPMappingTableWalker) Iter() iter.Seq2[snmp.OID, EntLPMappingTableRow] {
	return func(yield func(snmp.OID, EntLPMappingTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 1, 1).WireBytes()
		buffer := make(map[string]*EntLPMappingTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &EntLPMappingTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.EntLPPhysicalIndex = int32(v)
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := EntLPPhysicalIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.EntLPPhysicalIndex = dv
						}
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *EntLPMappingTableWalker) Err() error {
	return tw.rw.Err()
}

// entLPMappingTableT is the singleton type of EntLPMappingTable.
type entLPMappingTableT struct{}

// EntLPMappingTable is the descriptor for the entLPMappingTable table.
var EntLPMappingTable entLPMappingTableT

// Walk launches a BulkWalk over entLPMappingTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
func (entLPMappingTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *EntLPMappingTableWalker {
	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 1))
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() == 0 {
			continue
		}
		byCol[o.At(o.Len()-1)] = c
	}

	return &EntLPMappingTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// decodeEntLPMappingTableRow decodes one row of EntLPMappingTable from the supplied
// VarBinds. Each VarBind's OID determines which row field it populates
// (via the column's last sub-id). VarBinds with unknown column-ids are
// ignored. Absent columns leave their field at its zero value.
func decodeEntLPMappingTableRow(idx snmp.OID, vbs []snmp.VarBind) (EntLPMappingTableRow, error) {
	var row EntLPMappingTableRow
	row.Index = idx

	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 1, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 1:
			dv, derr := EntLPPhysicalIndex.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntLPPhysicalIndex = dv
		}
	}

	return row, nil
}

// equalEntLPMappingTableRow compares two EntLPMappingTableRow values for equality.
// Used by [snmp.Watcher] to compute ChangeKindModified emits. Field-by-
// field with the type-appropriate comparator (bytes.Equal for []byte,
// OID.Equal for OID, time.Time.Equal for time.Time, == for everything else).
func equalEntLPMappingTableRow(a EntLPMappingTableRow, b EntLPMappingTableRow) bool {
	return a.Index.Equal(b.Index) && a.EntLPPhysicalIndex == b.EntLPPhysicalIndex
}

// mergeEntLPMappingTableRow merges the values decoded from vbs into dst, leaving fields
// whose columns are not present in vbs unchanged. Used by [snmp.Watcher]
// to maintain per-row state under partial-column fetches (Counter-tier,
// Static-tier). Best-effort: individual VarBind decode failures are
// silently skipped rather than propagated, because partial-fetch ticks
// surface transient errors through [snmp.Watcher.LastTickErr] at the
// call-site granularity rather than per-VarBind.
func mergeEntLPMappingTableRow(dst *EntLPMappingTableRow, vbs []snmp.VarBind) {
	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 1, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 1:
			dv, derr := EntLPPhysicalIndex.Decode(vb)
			if derr == nil {
				dst.EntLPPhysicalIndex = dv
			}
		}
	}
}

// EntLPMappingTableWatcher is a table-aware Watcher over EntLPMappingTable.
// Construct via EntLPMappingTable.Watch(ctx, sess, cols, opts...).
type EntLPMappingTableWatcher struct {
	w *snmp.Watcher[EntLPMappingTableRow]
}

// Iter returns the range-over-func view of the Watcher's event stream.
// See [snmp.Watcher.Iter] for the contract.
func (tw *EntLPMappingTableWatcher) Iter() iter.Seq2[snmp.OID, snmp.WatchEvent[EntLPMappingTableRow]] {
	return tw.w.Iter()
}

// Err returns the underlying Watcher's terminal error, or nil if it
// completed naturally. See [snmp.Watcher.Err].
func (tw *EntLPMappingTableWatcher) Err() error {
	return tw.w.Err()
}

// Close signals the Watcher's tick goroutine to terminate. Idempotent.
// See [snmp.Watcher.Close].
func (tw *EntLPMappingTableWatcher) Close() error {
	return tw.w.Close()
}

// Fallback reports whether the Watcher has transitioned to fallback
// mode. See [snmp.Watcher.Fallback].
func (tw *EntLPMappingTableWatcher) Fallback() bool {
	return tw.w.Fallback()
}

// LastTickErr returns the most-recent transient per-tick error.
// See [snmp.Watcher.LastTickErr].
func (tw *EntLPMappingTableWatcher) LastTickErr() error {
	return tw.w.LastTickErr()
}

// TableRoot returns the OID of the table this Watcher operates over.
func (tw *EntLPMappingTableWatcher) TableRoot() snmp.OID {
	return tw.w.TableRoot()
}

// Watch opens a long-lived watch on EntLPMappingTable.
//
// cols selects the columns whose values are reported on every emitted
// event. opts override cadence bounds, per-column tier classifications,
// fallback behavior, and other policy — see [snmp.WatchOption] for the
// full set.
//
// cols is a slice rather than variadic because opts is variadic and
// Go forbids two variadic parameters.
//
// Events emitted on this stream:
//   - ChangeKindAdded    — Row populated, Prev always nil.
//   - ChangeKindModified — Row is current state. Prev is nil unless
//     the caller passed [snmp.WithPrevRow]; when
//     set, Prev carries the previous Row.
//   - ChangeKindRemoved  — Row carries the last-known state at the
//     time the row disappeared. Prev is nil.
//
// The returned Watcher must be Closed when the caller is done; iteration
// exit alone does not free the underlying goroutine until Close.
//
// On validation failure (invalid cadence bounds, tier override of the
// indicator column, etc.) Watch returns a Watcher whose Err() returns
// the cause immediately; range loops exit without emitting events and
// Close is a no-op.
func (entLPMappingTableT) Watch(ctx context.Context, sess snmp.Session, cols []snmp.AnyColumn, opts ...snmp.WatchOption) *EntLPMappingTableWatcher {
	allOpts := append([]snmp.WatchOption{snmp.WithTierLookup(ColumnTier)}, opts...)
	w, _ := snmp.NewWatcher[EntLPMappingTableRow](ctx, sess, EntLPMappingTableIndicator, cols, decodeEntLPMappingTableRow, equalEntLPMappingTableRow, mergeEntLPMappingTableRow, allOpts...)

	return &EntLPMappingTableWatcher{w: w}
}

// EntAliasMappingIdentifier is the column entAliasMappingIdentifier of table entAliasMappingTable.
// The value of this object identifies a particular conceptual row
// associated with the indicated entPhysicalIndex and entLogicalIndex pair.
// Because only physical ports are modeled in this table, only entries that
// represent interfaces or ports are allowed. If an ifEntry exists on
// behalf of a particular physical port, then this object should identify
// the associated ifEntry. For repeater ports, the appropriate row in the
// 'rptrPortGroupTable' should be identified instead. For example, suppose
// a physical port was represented by entPhysicalEntry.3,
// entLogicalEntry.15 existed for a repeater, and entLogicalEntry.22
// existed for a bridge. Then there might be two related instances of
// entAliasMappingIdentifier: entAliasMappingIdentifier.3.15 ==
// rptrPortGroupIndex.5.2 entAliasMappingIdentifier.3.22 == ifIndex.17 It
// is possible that other mappings (besides interfaces and repeater ports)
// may be defined in the future, as required. Bridge ports are identified
// by examining the Bridge MIB and appropriate ifEntries associated with
// each 'dot1dBasePort' and are thus not represented in this table.
var EntAliasMappingIdentifier = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 2, 1, 2), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// EntAliasMappingTableRow is one row of entAliasMappingTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk().
type EntAliasMappingTableRow struct {
	Index                     snmp.OID
	EntAliasMappingIdentifier snmp.OID
}

// EntAliasMappingTableWalker is a table-aware walker over entAliasMappingTable.
// Construct via EntAliasMappingTable.Walk(ctx, sess, cols...).
type EntAliasMappingTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *EntAliasMappingTableWalker) Iter() iter.Seq2[snmp.OID, EntAliasMappingTableRow] {
	return func(yield func(snmp.OID, EntAliasMappingTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 2, 1).WireBytes()
		buffer := make(map[string]*EntAliasMappingTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &EntAliasMappingTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 2:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := EntAliasMappingIdentifier.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.EntAliasMappingIdentifier = dv
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *EntAliasMappingTableWalker) Err() error {
	return tw.rw.Err()
}

// entAliasMappingTableT is the singleton type of EntAliasMappingTable.
type entAliasMappingTableT struct{}

// EntAliasMappingTable is the descriptor for the entAliasMappingTable table.
var EntAliasMappingTable entAliasMappingTableT

// Walk launches a BulkWalk over entAliasMappingTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
func (entAliasMappingTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *EntAliasMappingTableWalker {
	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 2))
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() == 0 {
			continue
		}
		byCol[o.At(o.Len()-1)] = c
	}

	return &EntAliasMappingTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// decodeEntAliasMappingTableRow decodes one row of EntAliasMappingTable from the supplied
// VarBinds. Each VarBind's OID determines which row field it populates
// (via the column's last sub-id). VarBinds with unknown column-ids are
// ignored. Absent columns leave their field at its zero value.
func decodeEntAliasMappingTableRow(idx snmp.OID, vbs []snmp.VarBind) (EntAliasMappingTableRow, error) {
	var row EntAliasMappingTableRow
	row.Index = idx

	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 2, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 2:
			dv, derr := EntAliasMappingIdentifier.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntAliasMappingIdentifier = dv
		}
	}

	return row, nil
}

// equalEntAliasMappingTableRow compares two EntAliasMappingTableRow values for equality.
// Used by [snmp.Watcher] to compute ChangeKindModified emits. Field-by-
// field with the type-appropriate comparator (bytes.Equal for []byte,
// OID.Equal for OID, time.Time.Equal for time.Time, == for everything else).
func equalEntAliasMappingTableRow(a EntAliasMappingTableRow, b EntAliasMappingTableRow) bool {
	return a.Index.Equal(b.Index) && a.EntAliasMappingIdentifier.Equal(b.EntAliasMappingIdentifier)
}

// mergeEntAliasMappingTableRow merges the values decoded from vbs into dst, leaving fields
// whose columns are not present in vbs unchanged. Used by [snmp.Watcher]
// to maintain per-row state under partial-column fetches (Counter-tier,
// Static-tier). Best-effort: individual VarBind decode failures are
// silently skipped rather than propagated, because partial-fetch ticks
// surface transient errors through [snmp.Watcher.LastTickErr] at the
// call-site granularity rather than per-VarBind.
func mergeEntAliasMappingTableRow(dst *EntAliasMappingTableRow, vbs []snmp.VarBind) {
	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 2, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 2:
			dv, derr := EntAliasMappingIdentifier.Decode(vb)
			if derr == nil {
				dst.EntAliasMappingIdentifier = dv
			}
		}
	}
}

// EntAliasMappingTableWatcher is a table-aware Watcher over EntAliasMappingTable.
// Construct via EntAliasMappingTable.Watch(ctx, sess, cols, opts...).
type EntAliasMappingTableWatcher struct {
	w *snmp.Watcher[EntAliasMappingTableRow]
}

// Iter returns the range-over-func view of the Watcher's event stream.
// See [snmp.Watcher.Iter] for the contract.
func (tw *EntAliasMappingTableWatcher) Iter() iter.Seq2[snmp.OID, snmp.WatchEvent[EntAliasMappingTableRow]] {
	return tw.w.Iter()
}

// Err returns the underlying Watcher's terminal error, or nil if it
// completed naturally. See [snmp.Watcher.Err].
func (tw *EntAliasMappingTableWatcher) Err() error {
	return tw.w.Err()
}

// Close signals the Watcher's tick goroutine to terminate. Idempotent.
// See [snmp.Watcher.Close].
func (tw *EntAliasMappingTableWatcher) Close() error {
	return tw.w.Close()
}

// Fallback reports whether the Watcher has transitioned to fallback
// mode. See [snmp.Watcher.Fallback].
func (tw *EntAliasMappingTableWatcher) Fallback() bool {
	return tw.w.Fallback()
}

// LastTickErr returns the most-recent transient per-tick error.
// See [snmp.Watcher.LastTickErr].
func (tw *EntAliasMappingTableWatcher) LastTickErr() error {
	return tw.w.LastTickErr()
}

// TableRoot returns the OID of the table this Watcher operates over.
func (tw *EntAliasMappingTableWatcher) TableRoot() snmp.OID {
	return tw.w.TableRoot()
}

// Watch opens a long-lived watch on EntAliasMappingTable.
//
// cols selects the columns whose values are reported on every emitted
// event. opts override cadence bounds, per-column tier classifications,
// fallback behavior, and other policy — see [snmp.WatchOption] for the
// full set.
//
// cols is a slice rather than variadic because opts is variadic and
// Go forbids two variadic parameters.
//
// Events emitted on this stream:
//   - ChangeKindAdded    — Row populated, Prev always nil.
//   - ChangeKindModified — Row is current state. Prev is nil unless
//     the caller passed [snmp.WithPrevRow]; when
//     set, Prev carries the previous Row.
//   - ChangeKindRemoved  — Row carries the last-known state at the
//     time the row disappeared. Prev is nil.
//
// The returned Watcher must be Closed when the caller is done; iteration
// exit alone does not free the underlying goroutine until Close.
//
// On validation failure (invalid cadence bounds, tier override of the
// indicator column, etc.) Watch returns a Watcher whose Err() returns
// the cause immediately; range loops exit without emitting events and
// Close is a no-op.
func (entAliasMappingTableT) Watch(ctx context.Context, sess snmp.Session, cols []snmp.AnyColumn, opts ...snmp.WatchOption) *EntAliasMappingTableWatcher {
	allOpts := append([]snmp.WatchOption{snmp.WithTierLookup(ColumnTier)}, opts...)
	w, _ := snmp.NewWatcher[EntAliasMappingTableRow](ctx, sess, EntAliasMappingTableIndicator, cols, decodeEntAliasMappingTableRow, equalEntAliasMappingTableRow, mergeEntAliasMappingTableRow, allOpts...)

	return &EntAliasMappingTableWatcher{w: w}
}

// EntPhysicalChildIndex is the column entPhysicalChildIndex of table entPhysicalContainsTable.
// The value of entPhysicalIndex for the contained physical entity.
var EntPhysicalChildIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 3, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// EntPhysicalContainsTableRow is one row of entPhysicalContainsTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk().
type EntPhysicalContainsTableRow struct {
	Index                 snmp.OID
	EntPhysicalChildIndex int32
}

// EntPhysicalContainsTableWalker is a table-aware walker over entPhysicalContainsTable.
// Construct via EntPhysicalContainsTable.Walk(ctx, sess, cols...).
type EntPhysicalContainsTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *EntPhysicalContainsTableWalker) Iter() iter.Seq2[snmp.OID, EntPhysicalContainsTableRow] {
	return func(yield func(snmp.OID, EntPhysicalContainsTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 3, 1).WireBytes()
		buffer := make(map[string]*EntPhysicalContainsTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &EntPhysicalContainsTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.EntPhysicalChildIndex = int32(v)
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := EntPhysicalChildIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.EntPhysicalChildIndex = dv
						}
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *EntPhysicalContainsTableWalker) Err() error {
	return tw.rw.Err()
}

// entPhysicalContainsTableT is the singleton type of EntPhysicalContainsTable.
type entPhysicalContainsTableT struct{}

// EntPhysicalContainsTable is the descriptor for the entPhysicalContainsTable table.
var EntPhysicalContainsTable entPhysicalContainsTableT

// Walk launches a BulkWalk over entPhysicalContainsTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
func (entPhysicalContainsTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *EntPhysicalContainsTableWalker {
	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 3))
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() == 0 {
			continue
		}
		byCol[o.At(o.Len()-1)] = c
	}

	return &EntPhysicalContainsTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// decodeEntPhysicalContainsTableRow decodes one row of EntPhysicalContainsTable from the supplied
// VarBinds. Each VarBind's OID determines which row field it populates
// (via the column's last sub-id). VarBinds with unknown column-ids are
// ignored. Absent columns leave their field at its zero value.
func decodeEntPhysicalContainsTableRow(idx snmp.OID, vbs []snmp.VarBind) (EntPhysicalContainsTableRow, error) {
	var row EntPhysicalContainsTableRow
	row.Index = idx

	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 3, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 1:
			dv, derr := EntPhysicalChildIndex.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.EntPhysicalChildIndex = dv
		}
	}

	return row, nil
}

// equalEntPhysicalContainsTableRow compares two EntPhysicalContainsTableRow values for equality.
// Used by [snmp.Watcher] to compute ChangeKindModified emits. Field-by-
// field with the type-appropriate comparator (bytes.Equal for []byte,
// OID.Equal for OID, time.Time.Equal for time.Time, == for everything else).
func equalEntPhysicalContainsTableRow(a EntPhysicalContainsTableRow, b EntPhysicalContainsTableRow) bool {
	return a.Index.Equal(b.Index) && a.EntPhysicalChildIndex == b.EntPhysicalChildIndex
}

// mergeEntPhysicalContainsTableRow merges the values decoded from vbs into dst, leaving fields
// whose columns are not present in vbs unchanged. Used by [snmp.Watcher]
// to maintain per-row state under partial-column fetches (Counter-tier,
// Static-tier). Best-effort: individual VarBind decode failures are
// silently skipped rather than propagated, because partial-fetch ticks
// surface transient errors through [snmp.Watcher.LastTickErr] at the
// call-site granularity rather than per-VarBind.
func mergeEntPhysicalContainsTableRow(dst *EntPhysicalContainsTableRow, vbs []snmp.VarBind) {
	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 3, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 1:
			dv, derr := EntPhysicalChildIndex.Decode(vb)
			if derr == nil {
				dst.EntPhysicalChildIndex = dv
			}
		}
	}
}

// EntPhysicalContainsTableWatcher is a table-aware Watcher over EntPhysicalContainsTable.
// Construct via EntPhysicalContainsTable.Watch(ctx, sess, cols, opts...).
type EntPhysicalContainsTableWatcher struct {
	w *snmp.Watcher[EntPhysicalContainsTableRow]
}

// Iter returns the range-over-func view of the Watcher's event stream.
// See [snmp.Watcher.Iter] for the contract.
func (tw *EntPhysicalContainsTableWatcher) Iter() iter.Seq2[snmp.OID, snmp.WatchEvent[EntPhysicalContainsTableRow]] {
	return tw.w.Iter()
}

// Err returns the underlying Watcher's terminal error, or nil if it
// completed naturally. See [snmp.Watcher.Err].
func (tw *EntPhysicalContainsTableWatcher) Err() error {
	return tw.w.Err()
}

// Close signals the Watcher's tick goroutine to terminate. Idempotent.
// See [snmp.Watcher.Close].
func (tw *EntPhysicalContainsTableWatcher) Close() error {
	return tw.w.Close()
}

// Fallback reports whether the Watcher has transitioned to fallback
// mode. See [snmp.Watcher.Fallback].
func (tw *EntPhysicalContainsTableWatcher) Fallback() bool {
	return tw.w.Fallback()
}

// LastTickErr returns the most-recent transient per-tick error.
// See [snmp.Watcher.LastTickErr].
func (tw *EntPhysicalContainsTableWatcher) LastTickErr() error {
	return tw.w.LastTickErr()
}

// TableRoot returns the OID of the table this Watcher operates over.
func (tw *EntPhysicalContainsTableWatcher) TableRoot() snmp.OID {
	return tw.w.TableRoot()
}

// Watch opens a long-lived watch on EntPhysicalContainsTable.
//
// cols selects the columns whose values are reported on every emitted
// event. opts override cadence bounds, per-column tier classifications,
// fallback behavior, and other policy — see [snmp.WatchOption] for the
// full set.
//
// cols is a slice rather than variadic because opts is variadic and
// Go forbids two variadic parameters.
//
// Events emitted on this stream:
//   - ChangeKindAdded    — Row populated, Prev always nil.
//   - ChangeKindModified — Row is current state. Prev is nil unless
//     the caller passed [snmp.WithPrevRow]; when
//     set, Prev carries the previous Row.
//   - ChangeKindRemoved  — Row carries the last-known state at the
//     time the row disappeared. Prev is nil.
//
// The returned Watcher must be Closed when the caller is done; iteration
// exit alone does not free the underlying goroutine until Close.
//
// On validation failure (invalid cadence bounds, tier override of the
// indicator column, etc.) Watch returns a Watcher whose Err() returns
// the cause immediately; range loops exit without emitting events and
// Close is a no-op.
func (entPhysicalContainsTableT) Watch(ctx context.Context, sess snmp.Session, cols []snmp.AnyColumn, opts ...snmp.WatchOption) *EntPhysicalContainsTableWatcher {
	allOpts := append([]snmp.WatchOption{snmp.WithTierLookup(ColumnTier)}, opts...)
	w, _ := snmp.NewWatcher[EntPhysicalContainsTableRow](ctx, sess, EntPhysicalContainsTableIndicator, cols, decodeEntPhysicalContainsTableRow, equalEntPhysicalContainsTableRow, mergeEntPhysicalContainsTableRow, allOpts...)

	return &EntPhysicalContainsTableWatcher{w: w}
}

// eNTITYMIBOIDDispatch maps column wire keys ([snmp.OID.WireKey]) to their
// typed AnyColumn for fast
// lookup during table-walk decoding. Per-MIB-module — no global
// registry; cross-package callers should consult OIDDispatch().
var eNTITYMIBOIDDispatch = map[string]snmp.AnyColumn{
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 10).WireKey(): EntPhysicalSoftwareRev,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 11).WireKey(): EntPhysicalSerialNum,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 12).WireKey(): EntPhysicalMfgName,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 13).WireKey(): EntPhysicalModelName,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 14).WireKey(): EntPhysicalAlias,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 15).WireKey(): EntPhysicalAssetID,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 16).WireKey(): EntPhysicalIsFRU,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 17).WireKey(): EntPhysicalMfgDate,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 18).WireKey(): EntPhysicalUris,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 19).WireKey(): EntPhysicalUUID,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 2).WireKey():  EntPhysicalDescr,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 3).WireKey():  EntPhysicalVendorType,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 4).WireKey():  EntPhysicalContainedIn,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 5).WireKey():  EntPhysicalClass,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 6).WireKey():  EntPhysicalParentRelPos,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 7).WireKey():  EntPhysicalName,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 8).WireKey():  EntPhysicalHardwareRev,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 9).WireKey():  EntPhysicalFirmwareRev,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 2).WireKey():  EntLogicalDescr,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 3).WireKey():  EntLogicalType,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 4).WireKey():  EntLogicalCommunity,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 5).WireKey():  EntLogicalTAddress,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 6).WireKey():  EntLogicalTDomain,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 7).WireKey():  EntLogicalContextEngineID,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 8).WireKey():  EntLogicalContextName,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 1, 1, 1).WireKey():  EntLPPhysicalIndex,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 2, 1, 2).WireKey():  EntAliasMappingIdentifier,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 3, 1, 1).WireKey():  EntPhysicalChildIndex,
}

// OIDDispatch returns a shallow copy of the package's wire-key →
// AnyColumn map. The copy isolates callers from accidental mutation
// of the package-internal map (#12).
func OIDDispatch() map[string]snmp.AnyColumn {
	out := make(map[string]snmp.AnyColumn, len(eNTITYMIBOIDDispatch))
	for k, v := range eNTITYMIBOIDDispatch {
		out[k] = v
	}
	return out
}

// eNTITYMIBColumnTiers maps column wire keys ([snmp.OID.WireKey]) to their
// snmp.Tier classification.
// Tier classification drives the Watcher's scheduler — counters fire
// on their own cadence, state columns refresh on indicator advance,
// and indicator columns drive the change-detection probe itself.
// See snmp.Tier and snmp.Watcher for the consumption contract.
var eNTITYMIBColumnTiers = map[string]snmp.Tier{
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 10).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 11).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 12).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 13).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 14).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 15).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 16).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 17).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 18).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 19).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 2).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 3).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 4).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 5).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 6).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 7).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 8).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1, 1, 9).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 2).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 3).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 4).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 5).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 6).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 7).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1, 1, 8).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 1, 1, 1).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 2, 1, 2).WireKey():  snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 3, 1, 1).WireKey():  snmp.TierState,
}

// ColumnTier returns the codegen-classified [snmp.Tier] for col, or
// snmp.TierUnknown when col's OID is not known to this package. The
// generated Watch methods install this function via
// [snmp.WithTierLookup]; returning TierUnknown lets the runtime fall
// back to its wire-Kind heuristic for columns this package does not
// classify. Caller overrides via [snmp.WithColumnTier] still win.
func ColumnTier(col snmp.AnyColumn) snmp.Tier {
	if col == nil {
		return snmp.TierUnknown
	}
	t, ok := eNTITYMIBColumnTiers[col.Key()]
	if !ok {
		return snmp.TierUnknown
	}
	return t
}

// EntPhysicalTableIndicator is the scalar change indicator for entPhysicalTable.
// The Watcher Gets entLastChangeTime on each tick; when the value
// advances the full table is walked and diffed against the snapshot.
// See [snmp.NewScalarIndicator] and [snmp.Watcher] for the contract.
// Declared in mibgen.yaml under modules.<name>.indicators.
var EntPhysicalTableIndicator = snmp.MustChangeIndicator(snmp.NewScalarIndicator(snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 4, 1), snmp.KindUinteger32, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 1, 1)}))

// EntLogicalTableIndicator is the scalar change indicator for entLogicalTable.
// The Watcher Gets entLastChangeTime on each tick; when the value
// advances the full table is walked and diffed against the snapshot.
// See [snmp.NewScalarIndicator] and [snmp.Watcher] for the contract.
// Declared in mibgen.yaml under modules.<name>.indicators.
var EntLogicalTableIndicator = snmp.MustChangeIndicator(snmp.NewScalarIndicator(snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 4, 1), snmp.KindUinteger32, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 2, 1)}))

// EntLPMappingTableIndicator is the scalar change indicator for entLPMappingTable.
// The Watcher Gets entLastChangeTime on each tick; when the value
// advances the full table is walked and diffed against the snapshot.
// See [snmp.NewScalarIndicator] and [snmp.Watcher] for the contract.
// Declared in mibgen.yaml under modules.<name>.indicators.
var EntLPMappingTableIndicator = snmp.MustChangeIndicator(snmp.NewScalarIndicator(snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 4, 1), snmp.KindUinteger32, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 1)}))

// EntAliasMappingTableIndicator is the scalar change indicator for entAliasMappingTable.
// The Watcher Gets entLastChangeTime on each tick; when the value
// advances the full table is walked and diffed against the snapshot.
// See [snmp.NewScalarIndicator] and [snmp.Watcher] for the contract.
// Declared in mibgen.yaml under modules.<name>.indicators.
var EntAliasMappingTableIndicator = snmp.MustChangeIndicator(snmp.NewScalarIndicator(snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 4, 1), snmp.KindUinteger32, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 2)}))

// EntPhysicalContainsTableIndicator is the scalar change indicator for entPhysicalContainsTable.
// The Watcher Gets entLastChangeTime on each tick; when the value
// advances the full table is walked and diffed against the snapshot.
// See [snmp.NewScalarIndicator] and [snmp.Watcher] for the contract.
// Declared in mibgen.yaml under modules.<name>.indicators.
var EntPhysicalContainsTableIndicator = snmp.MustChangeIndicator(snmp.NewScalarIndicator(snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 4, 1), snmp.KindUinteger32, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 47, 1, 3, 3)}))
