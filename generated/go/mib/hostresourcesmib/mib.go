// Code generated by mibgen; DO NOT EDIT.
//
// Source MIB:    HOST-RESOURCES-MIB
// Source path:   spec/mib/ietf/HOST-RESOURCES-MIB
// Source SHA-256: 0406b893dcfacc370a1ae04ccfe6ffdecb15f93e3377a1a5ded2bbde5d944165
//
// Regenerate with `go generate .` at the repository root.

package hostresourcesmib

import (
	"bytes"
	"context"
	"fmt"
	"iter"
	"time"

	snmp "go.aledante.io/FlowSeer/src/common/snmp"
	ae "go.aledante.io/ae"
)

// HrDeviceStatusValue is the SMI enum hrDeviceStatus (inline).
// The current operational state of the device described by this row of the
// table. A value unknown(1) indicates that the current state of the device
// is unknown. running(2) indicates that the device is up and running and
// that no unusual error conditions are known. The warning(3) state
// indicates that agent has been informed of an unusual error condition by
// the operational software (e.g., a disk device driver) but that the
// device is still 'operational'. An example would be a high number of soft
// errors on a disk. A value of testing(4), indicates that the device is
// not available for use because it is in the testing state. The state of
// down(5) is used only when the agent has been informed that the device is
// not available for any use.
type HrDeviceStatusValue int32

const (
	HrDeviceStatusValueUnknown HrDeviceStatusValue = 1
	HrDeviceStatusValueRunning HrDeviceStatusValue = 2
	HrDeviceStatusValueWarning HrDeviceStatusValue = 3
	HrDeviceStatusValueTesting HrDeviceStatusValue = 4
	HrDeviceStatusValueDown    HrDeviceStatusValue = 5
)

func (v HrDeviceStatusValue) String() string {
	switch v {
	case HrDeviceStatusValueUnknown:
		return "unknown"
	case HrDeviceStatusValueRunning:
		return "running"
	case HrDeviceStatusValueWarning:
		return "warning"
	case HrDeviceStatusValueTesting:
		return "testing"
	case HrDeviceStatusValueDown:
		return "down"
	}

	return fmt.Sprintf("HrDeviceStatusValue(%d)", v)
}

// HrDiskStorageAccessValue is the SMI enum hrDiskStorageAccess (inline).
// An indication if this long-term storage device is readable and writable
// or only readable. This should reflect the media type, any write-protect
// mechanism, and any device configuration that affects the entire device.
type HrDiskStorageAccessValue int32

const (
	HrDiskStorageAccessValueReadWrite HrDiskStorageAccessValue = 1
	HrDiskStorageAccessValueReadOnly  HrDiskStorageAccessValue = 2
)

func (v HrDiskStorageAccessValue) String() string {
	switch v {
	case HrDiskStorageAccessValueReadWrite:
		return "readWrite"
	case HrDiskStorageAccessValueReadOnly:
		return "readOnly"
	}

	return fmt.Sprintf("HrDiskStorageAccessValue(%d)", v)
}

// HrDiskStorageMediaValue is the SMI enum hrDiskStorageMedia (inline).
// An indication of the type of media used in this long- term storage
// device.
type HrDiskStorageMediaValue int32

const (
	HrDiskStorageMediaValueOther           HrDiskStorageMediaValue = 1
	HrDiskStorageMediaValueUnknown         HrDiskStorageMediaValue = 2
	HrDiskStorageMediaValueHardDisk        HrDiskStorageMediaValue = 3
	HrDiskStorageMediaValueFloppyDisk      HrDiskStorageMediaValue = 4
	HrDiskStorageMediaValueOpticalDiskROM  HrDiskStorageMediaValue = 5
	HrDiskStorageMediaValueOpticalDiskWORM HrDiskStorageMediaValue = 6
	HrDiskStorageMediaValueOpticalDiskRW   HrDiskStorageMediaValue = 7
	HrDiskStorageMediaValueRamDisk         HrDiskStorageMediaValue = 8
)

func (v HrDiskStorageMediaValue) String() string {
	switch v {
	case HrDiskStorageMediaValueOther:
		return "other"
	case HrDiskStorageMediaValueUnknown:
		return "unknown"
	case HrDiskStorageMediaValueHardDisk:
		return "hardDisk"
	case HrDiskStorageMediaValueFloppyDisk:
		return "floppyDisk"
	case HrDiskStorageMediaValueOpticalDiskROM:
		return "opticalDiskROM"
	case HrDiskStorageMediaValueOpticalDiskWORM:
		return "opticalDiskWORM"
	case HrDiskStorageMediaValueOpticalDiskRW:
		return "opticalDiskRW"
	case HrDiskStorageMediaValueRamDisk:
		return "ramDisk"
	}

	return fmt.Sprintf("HrDiskStorageMediaValue(%d)", v)
}

// HrFSAccessValue is the SMI enum hrFSAccess (inline).
// An indication if this file system is logically configured by the
// operating system to be readable and writable or only readable. This does
// not represent any local access-control policy, except one that is
// applied to the file system as a whole.
type HrFSAccessValue int32

const (
	HrFSAccessValueReadWrite HrFSAccessValue = 1
	HrFSAccessValueReadOnly  HrFSAccessValue = 2
)

func (v HrFSAccessValue) String() string {
	switch v {
	case HrFSAccessValueReadWrite:
		return "readWrite"
	case HrFSAccessValueReadOnly:
		return "readOnly"
	}

	return fmt.Sprintf("HrFSAccessValue(%d)", v)
}

// HrPrinterStatusValue is the SMI enum hrPrinterStatus (inline).
// The current status of this printer device.
type HrPrinterStatusValue int32

const (
	HrPrinterStatusValueOther    HrPrinterStatusValue = 1
	HrPrinterStatusValueUnknown  HrPrinterStatusValue = 2
	HrPrinterStatusValueIdle     HrPrinterStatusValue = 3
	HrPrinterStatusValuePrinting HrPrinterStatusValue = 4
	HrPrinterStatusValueWarmup   HrPrinterStatusValue = 5
)

func (v HrPrinterStatusValue) String() string {
	switch v {
	case HrPrinterStatusValueOther:
		return "other"
	case HrPrinterStatusValueUnknown:
		return "unknown"
	case HrPrinterStatusValueIdle:
		return "idle"
	case HrPrinterStatusValuePrinting:
		return "printing"
	case HrPrinterStatusValueWarmup:
		return "warmup"
	}

	return fmt.Sprintf("HrPrinterStatusValue(%d)", v)
}

// HrSWInstalledTypeValue is the SMI enum hrSWInstalledType (inline).
// The type of this software.
type HrSWInstalledTypeValue int32

const (
	HrSWInstalledTypeValueUnknown         HrSWInstalledTypeValue = 1
	HrSWInstalledTypeValueOperatingSystem HrSWInstalledTypeValue = 2
	HrSWInstalledTypeValueDeviceDriver    HrSWInstalledTypeValue = 3
	HrSWInstalledTypeValueApplication     HrSWInstalledTypeValue = 4
)

func (v HrSWInstalledTypeValue) String() string {
	switch v {
	case HrSWInstalledTypeValueUnknown:
		return "unknown"
	case HrSWInstalledTypeValueOperatingSystem:
		return "operatingSystem"
	case HrSWInstalledTypeValueDeviceDriver:
		return "deviceDriver"
	case HrSWInstalledTypeValueApplication:
		return "application"
	}

	return fmt.Sprintf("HrSWInstalledTypeValue(%d)", v)
}

// HrSWRunStatusValue is the SMI enum hrSWRunStatus (inline).
// The status of this running piece of software. Setting this value to
// invalid(4) shall cause this software to stop running and to be unloaded.
// Sets to other values are not valid.
type HrSWRunStatusValue int32

const (
	HrSWRunStatusValueRunning     HrSWRunStatusValue = 1
	HrSWRunStatusValueRunnable    HrSWRunStatusValue = 2
	HrSWRunStatusValueNotRunnable HrSWRunStatusValue = 3
	HrSWRunStatusValueInvalid     HrSWRunStatusValue = 4
)

func (v HrSWRunStatusValue) String() string {
	switch v {
	case HrSWRunStatusValueRunning:
		return "running"
	case HrSWRunStatusValueRunnable:
		return "runnable"
	case HrSWRunStatusValueNotRunnable:
		return "notRunnable"
	case HrSWRunStatusValueInvalid:
		return "invalid"
	}

	return fmt.Sprintf("HrSWRunStatusValue(%d)", v)
}

// HrSWRunTypeValue is the SMI enum hrSWRunType (inline).
// The type of this software.
type HrSWRunTypeValue int32

const (
	HrSWRunTypeValueUnknown         HrSWRunTypeValue = 1
	HrSWRunTypeValueOperatingSystem HrSWRunTypeValue = 2
	HrSWRunTypeValueDeviceDriver    HrSWRunTypeValue = 3
	HrSWRunTypeValueApplication     HrSWRunTypeValue = 4
)

func (v HrSWRunTypeValue) String() string {
	switch v {
	case HrSWRunTypeValueUnknown:
		return "unknown"
	case HrSWRunTypeValueOperatingSystem:
		return "operatingSystem"
	case HrSWRunTypeValueDeviceDriver:
		return "deviceDriver"
	case HrSWRunTypeValueApplication:
		return "application"
	}

	return fmt.Sprintf("HrSWRunTypeValue(%d)", v)
}

// HrSystemUptimeGet reads the SMIv2 scalar hrSystemUptime.
// The amount of time since this host was last initialized. Note that this
// is different from sysUpTime in the SNMPv2-MIB [RFC1907] because
// sysUpTime is the uptime of the network management portion of the system.
func HrSystemUptimeGet(ctx context.Context, sess snmp.Session) (uint32, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 1, 1, 0)})
	if err != nil {
		return 0, err
	}

	if len(vbs) == 0 {
		return 0, ae.Msg("empty Get response for hrSystemUptime")
	}

	return func(vb snmp.VarBind) (uint32, error) {
		return snmp.DecodeUint32(vb)
	}(vbs[0])
}

// HrSystemDateGet reads the SMIv2 scalar hrSystemDate.
// The host's notion of the local date and time of day.
func HrSystemDateGet(ctx context.Context, sess snmp.Session) (time.Time, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 1, 2, 0)})
	if err != nil {
		return time.Time{}, err
	}

	if len(vbs) == 0 {
		return time.Time{}, ae.Msg("empty Get response for hrSystemDate")
	}

	return func(vb snmp.VarBind) (time.Time, error) {
		return snmp.DecodeDateAndTime(vb)
	}(vbs[0])
}

// HrSystemInitialLoadDeviceGet reads the SMIv2 scalar hrSystemInitialLoadDevice.
// The index of the hrDeviceEntry for the device from which this host is
// configured to load its initial operating system configuration (i.e.,
// which operating system code and/or boot parameters). Note that writing
// to this object just changes the configuration that will be used the next
// time the operating system is loaded and does not actually cause the
// reload to occur.
func HrSystemInitialLoadDeviceGet(ctx context.Context, sess snmp.Session) (int32, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 1, 3, 0)})
	if err != nil {
		return 0, err
	}

	if len(vbs) == 0 {
		return 0, ae.Msg("empty Get response for hrSystemInitialLoadDevice")
	}

	return func(vb snmp.VarBind) (int32, error) {
		return snmp.DecodeInt32(vb)
	}(vbs[0])
}

// HrSystemInitialLoadParametersGet reads the SMIv2 scalar hrSystemInitialLoadParameters.
// This object contains the parameters (e.g. a pathname and parameter)
// supplied to the load device when requesting the initial operating system
// configuration from that device. Note that writing to this object just
// changes the configuration that will be used the next time the operating
// system is loaded and does not actually cause the reload to occur.
func HrSystemInitialLoadParametersGet(ctx context.Context, sess snmp.Session) ([]byte, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 1, 4, 0)})
	if err != nil {
		return nil, err
	}

	if len(vbs) == 0 {
		return nil, ae.Msg("empty Get response for hrSystemInitialLoadParameters")
	}

	return func(vb snmp.VarBind) ([]byte, error) {
		return snmp.DecodeBytes(vb)
	}(vbs[0])
}

// HrSystemNumUsersGet reads the SMIv2 scalar hrSystemNumUsers.
// The number of user sessions for which this host is storing state
// information. A session is a collection of processes requiring a single
// act of user authentication and possibly subject to collective job
// control.
func HrSystemNumUsersGet(ctx context.Context, sess snmp.Session) (uint32, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 1, 5, 0)})
	if err != nil {
		return 0, err
	}

	if len(vbs) == 0 {
		return 0, ae.Msg("empty Get response for hrSystemNumUsers")
	}

	return func(vb snmp.VarBind) (uint32, error) {
		return snmp.DecodeUint32(vb)
	}(vbs[0])
}

// HrSystemProcessesGet reads the SMIv2 scalar hrSystemProcesses.
// The number of process contexts currently loaded or running on this
// system.
func HrSystemProcessesGet(ctx context.Context, sess snmp.Session) (uint32, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 1, 6, 0)})
	if err != nil {
		return 0, err
	}

	if len(vbs) == 0 {
		return 0, ae.Msg("empty Get response for hrSystemProcesses")
	}

	return func(vb snmp.VarBind) (uint32, error) {
		return snmp.DecodeUint32(vb)
	}(vbs[0])
}

// HrSystemMaxProcessesGet reads the SMIv2 scalar hrSystemMaxProcesses.
// The maximum number of process contexts this system can support. If there
// is no fixed maximum, the value should be zero. On systems that have a
// fixed maximum, this object can help diagnose failures that occur when
// this maximum is reached.
func HrSystemMaxProcessesGet(ctx context.Context, sess snmp.Session) (int32, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 1, 7, 0)})
	if err != nil {
		return 0, err
	}

	if len(vbs) == 0 {
		return 0, ae.Msg("empty Get response for hrSystemMaxProcesses")
	}

	return func(vb snmp.VarBind) (int32, error) {
		return snmp.DecodeInt32(vb)
	}(vbs[0])
}

// HrMemorySizeGet reads the SMIv2 scalar hrMemorySize.
// The amount of physical read-write main memory, typically RAM, contained
// by the host.
func HrMemorySizeGet(ctx context.Context, sess snmp.Session) (int32, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 2, 0)})
	if err != nil {
		return 0, err
	}

	if len(vbs) == 0 {
		return 0, ae.Msg("empty Get response for hrMemorySize")
	}

	return func(vb snmp.VarBind) (int32, error) {
		return snmp.DecodeInt32(vb)
	}(vbs[0])
}

// HrSWOSIndexGet reads the SMIv2 scalar hrSWOSIndex.
// The value of the hrSWRunIndex for the hrSWRunEntry that represents the
// primary operating system running on this host. This object is useful for
// quickly and uniquely identifying that primary operating system.
func HrSWOSIndexGet(ctx context.Context, sess snmp.Session) (int32, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 1, 0)})
	if err != nil {
		return 0, err
	}

	if len(vbs) == 0 {
		return 0, ae.Msg("empty Get response for hrSWOSIndex")
	}

	return func(vb snmp.VarBind) (int32, error) {
		return snmp.DecodeInt32(vb)
	}(vbs[0])
}

// HrSWInstalledLastChangeGet reads the SMIv2 scalar hrSWInstalledLastChange.
// The value of sysUpTime when an entry in the hrSWInstalledTable was last
// added, renamed, or deleted. Because this table is likely to contain many
// entries, polling of this object allows a management station to determine
// when re-downloading of the table might be useful.
func HrSWInstalledLastChangeGet(ctx context.Context, sess snmp.Session) (uint32, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 1, 0)})
	if err != nil {
		return 0, err
	}

	if len(vbs) == 0 {
		return 0, ae.Msg("empty Get response for hrSWInstalledLastChange")
	}

	return func(vb snmp.VarBind) (uint32, error) {
		return snmp.DecodeUint32(vb)
	}(vbs[0])
}

// HrSWInstalledLastUpdateTimeGet reads the SMIv2 scalar hrSWInstalledLastUpdateTime.
// The value of sysUpTime when the hrSWInstalledTable was last completely
// updated. Because caching of this data will be a popular implementation
// strategy, retrieval of this object allows a management station to obtain
// a guarantee that no data in this table is older than the indicated time.
func HrSWInstalledLastUpdateTimeGet(ctx context.Context, sess snmp.Session) (uint32, error) {
	vbs, err := sess.Get(ctx, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 2, 0)})
	if err != nil {
		return 0, err
	}

	if len(vbs) == 0 {
		return 0, ae.Msg("empty Get response for hrSWInstalledLastUpdateTime")
	}

	return func(vb snmp.VarBind) (uint32, error) {
		return snmp.DecodeUint32(vb)
	}(vbs[0])
}

// HrStorageIndex is the column hrStorageIndex of table hrStorageTable.
// A unique value for each logical storage area contained by the host.
var HrStorageIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrStorageType is the column hrStorageType of table hrStorageTable.
// The type of storage represented by this entry.
var HrStorageType = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 2), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// HrStorageDescr is the column hrStorageDescr of table hrStorageTable.
// A description of the type and instance of the storage described by this
// entry.
var HrStorageDescr = snmp.NewColumn[string](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 3), snmp.KindOctetString, func(vb snmp.VarBind) (string, error) {
	return snmp.DecodeDisplayString(vb)
})

// HrStorageAllocationUnits is the column hrStorageAllocationUnits of table hrStorageTable.
// The size, in bytes, of the data objects allocated from this pool. If
// this entry is monitoring sectors, blocks, buffers, or packets, for
// example, this number will commonly be greater than one. Otherwise this
// number will typically be one.
var HrStorageAllocationUnits = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 4), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrStorageSize is the column hrStorageSize of table hrStorageTable.
// The size of the storage represented by this entry, in units of
// hrStorageAllocationUnits. This object is writable to allow remote
// configuration of the size of the storage area in those cases where such
// an operation makes sense and is possible on the underlying system. For
// example, the amount of main memory allocated to a buffer pool might be
// modified or the amount of disk space allocated to virtual memory might
// be modified.
var HrStorageSize = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 5), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrStorageUsed is the column hrStorageUsed of table hrStorageTable.
// The amount of the storage represented by this entry that is allocated,
// in units of hrStorageAllocationUnits.
var HrStorageUsed = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 6), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrStorageAllocationFailures is the column hrStorageAllocationFailures of table hrStorageTable.
// The number of requests for storage represented by this entry that could
// not be honored due to not enough storage. It should be noted that as
// this object has a SYNTAX of Counter32, that it does not have a defined
// initial value. However, it is recommended that this object be
// initialized to zero, even though management stations must not depend on
// such an initialization.
var HrStorageAllocationFailures = snmp.NewColumn[uint32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 7), snmp.KindCounter32, func(vb snmp.VarBind) (uint32, error) {
	return snmp.DecodeUint32(vb)
})

// HrStorageTableRow is one row of hrStorageTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrStorageTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrStorageTableRow struct {
	Index                       snmp.OID
	HrStorageIndex              int32
	HrStorageType               snmp.OID
	HrStorageDescr              string
	HrStorageAllocationUnits    int32
	HrStorageSize               int32
	HrStorageUsed               int32
	HrStorageAllocationFailures uint32

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrStorageTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrStorageIndex.Key():
		return r.observed[0]&(1<<0) != 0
	case HrStorageType.Key():
		return r.observed[0]&(1<<1) != 0
	case HrStorageDescr.Key():
		return r.observed[0]&(1<<2) != 0
	case HrStorageAllocationUnits.Key():
		return r.observed[0]&(1<<3) != 0
	case HrStorageSize.Key():
		return r.observed[0]&(1<<4) != 0
	case HrStorageUsed.Key():
		return r.observed[0]&(1<<5) != 0
	case HrStorageAllocationFailures.Key():
		return r.observed[0]&(1<<6) != 0
	}

	return false
}

// HrStorageTableWalker is a table-aware walker over hrStorageTable.
// Construct via HrStorageTable.Walk(ctx, sess, cols...).
type HrStorageTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrStorageTableWalker) Iter() iter.Seq2[snmp.OID, HrStorageTableRow] {
	return func(yield func(snmp.OID, HrStorageTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1).WireBytes()
		buffer := make(map[string]*HrStorageTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrStorageTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrStorageIndex = int32(v)
					row.observed[0] |= 1 << 0
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrStorageIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrStorageIndex = dv
							row.observed[0] |= 1 << 0
						}
					}
				}
			case 2:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrStorageType.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrStorageType = dv
						row.observed[0] |= 1 << 1
					}
				}
			case 3:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrStorageDescr.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrStorageDescr = dv
						row.observed[0] |= 1 << 2
					}
				}
			case 4:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrStorageAllocationUnits = int32(v)
					row.observed[0] |= 1 << 3
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrStorageAllocationUnits.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrStorageAllocationUnits = dv
							row.observed[0] |= 1 << 3
						}
					}
				}
			case 5:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrStorageSize = int32(v)
					row.observed[0] |= 1 << 4
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrStorageSize.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrStorageSize = dv
							row.observed[0] |= 1 << 4
						}
					}
				}
			case 6:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrStorageUsed = int32(v)
					row.observed[0] |= 1 << 5
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrStorageUsed.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrStorageUsed = dv
							row.observed[0] |= 1 << 5
						}
					}
				}
			case 7:
				if v, okRaw := snmp.RawCounter32(rv); okRaw {
					row.HrStorageAllocationFailures = uint32(v)
					row.observed[0] |= 1 << 6
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrStorageAllocationFailures.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrStorageAllocationFailures = dv
							row.observed[0] |= 1 << 6
						}
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrStorageTableWalker) Err() error {
	return tw.rw.Err()
}

// hrStorageTableT is the singleton type of HrStorageTable.
type hrStorageTableT struct{}

// HrStorageTable is the descriptor for the hrStorageTable table.
var HrStorageTable hrStorageTableT

// Walk launches a BulkWalk over hrStorageTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrStorageTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrStorageTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrStorageTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrStorageTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrStorageTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3))

	return &HrStorageTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// HrDeviceIndex is the column hrDeviceIndex of table hrDeviceTable.
// A unique value for each device contained by the host. The value for each
// device must remain constant at least from one re-initialization of the
// agent to the next re-initialization.
var HrDeviceIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrDeviceType is the column hrDeviceType of table hrDeviceTable.
// An indication of the type of device. If this value is `hrDeviceProcessor
// { hrDeviceTypes 3 }' then an entry exists in the hrProcessorTable which
// corresponds to this device. If this value is `hrDeviceNetwork {
// hrDeviceTypes 4 }', then an entry exists in the hrNetworkTable which
// corresponds to this device. If this value is `hrDevicePrinter {
// hrDeviceTypes 5 }', then an entry exists in the hrPrinterTable which
// corresponds to this device. If this value is `hrDeviceDiskStorage {
// hrDeviceTypes 6 }', then an entry exists in the hrDiskStorageTable which
// corresponds to this device.
var HrDeviceType = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 2), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// HrDeviceDescr is the column hrDeviceDescr of table hrDeviceTable.
// A textual description of this device, including the device's
// manufacturer and revision, and optionally, its serial number.
var HrDeviceDescr = snmp.NewColumn[string](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 3), snmp.KindOctetString, func(vb snmp.VarBind) (string, error) {
	return snmp.DecodeDisplayString(vb)
})

// HrDeviceID is the column hrDeviceID of table hrDeviceTable.
// The product ID for this device.
var HrDeviceID = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 4), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// HrDeviceStatus is the column hrDeviceStatus of table hrDeviceTable.
// The current operational state of the device described by this row of the
// table. A value unknown(1) indicates that the current state of the device
// is unknown. running(2) indicates that the device is up and running and
// that no unusual error conditions are known. The warning(3) state
// indicates that agent has been informed of an unusual error condition by
// the operational software (e.g., a disk device driver) but that the
// device is still 'operational'. An example would be a high number of soft
// errors on a disk. A value of testing(4), indicates that the device is
// not available for use because it is in the testing state. The state of
// down(5) is used only when the agent has been informed that the device is
// not available for any use.
var HrDeviceStatus = snmp.NewColumn[HrDeviceStatusValue](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 5), snmp.KindInteger32, func(vb snmp.VarBind) (HrDeviceStatusValue, error) {
	v, err := snmp.DecodeInt32(vb)
	if err != nil {
		return HrDeviceStatusValue(0), err
	}
	return HrDeviceStatusValue(v), nil
})

// HrDeviceErrors is the column hrDeviceErrors of table hrDeviceTable.
// The number of errors detected on this device. It should be noted that as
// this object has a SYNTAX of Counter32, that it does not have a defined
// initial value. However, it is recommended that this object be
// initialized to zero, even though management stations must not depend on
// such an initialization.
var HrDeviceErrors = snmp.NewColumn[uint32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 6), snmp.KindCounter32, func(vb snmp.VarBind) (uint32, error) {
	return snmp.DecodeUint32(vb)
})

// HrDeviceTableRow is one row of hrDeviceTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrDeviceTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrDeviceTableRow struct {
	Index          snmp.OID
	HrDeviceIndex  int32
	HrDeviceType   snmp.OID
	HrDeviceDescr  string
	HrDeviceID     snmp.OID
	HrDeviceStatus HrDeviceStatusValue
	HrDeviceErrors uint32

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrDeviceTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrDeviceIndex.Key():
		return r.observed[0]&(1<<0) != 0
	case HrDeviceType.Key():
		return r.observed[0]&(1<<1) != 0
	case HrDeviceDescr.Key():
		return r.observed[0]&(1<<2) != 0
	case HrDeviceID.Key():
		return r.observed[0]&(1<<3) != 0
	case HrDeviceStatus.Key():
		return r.observed[0]&(1<<4) != 0
	case HrDeviceErrors.Key():
		return r.observed[0]&(1<<5) != 0
	}

	return false
}

// HrDeviceTableWalker is a table-aware walker over hrDeviceTable.
// Construct via HrDeviceTable.Walk(ctx, sess, cols...).
type HrDeviceTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrDeviceTableWalker) Iter() iter.Seq2[snmp.OID, HrDeviceTableRow] {
	return func(yield func(snmp.OID, HrDeviceTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1).WireBytes()
		buffer := make(map[string]*HrDeviceTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrDeviceTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrDeviceIndex = int32(v)
					row.observed[0] |= 1 << 0
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrDeviceIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrDeviceIndex = dv
							row.observed[0] |= 1 << 0
						}
					}
				}
			case 2:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrDeviceType.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrDeviceType = dv
						row.observed[0] |= 1 << 1
					}
				}
			case 3:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrDeviceDescr.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrDeviceDescr = dv
						row.observed[0] |= 1 << 2
					}
				}
			case 4:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrDeviceID.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrDeviceID = dv
						row.observed[0] |= 1 << 3
					}
				}
			case 5:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrDeviceStatus = HrDeviceStatusValue(v)
					row.observed[0] |= 1 << 4
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrDeviceStatus.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrDeviceStatus = dv
							row.observed[0] |= 1 << 4
						}
					}
				}
			case 6:
				if v, okRaw := snmp.RawCounter32(rv); okRaw {
					row.HrDeviceErrors = uint32(v)
					row.observed[0] |= 1 << 5
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrDeviceErrors.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrDeviceErrors = dv
							row.observed[0] |= 1 << 5
						}
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrDeviceTableWalker) Err() error {
	return tw.rw.Err()
}

// hrDeviceTableT is the singleton type of HrDeviceTable.
type hrDeviceTableT struct{}

// HrDeviceTable is the descriptor for the hrDeviceTable table.
var HrDeviceTable hrDeviceTableT

// Walk launches a BulkWalk over hrDeviceTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrDeviceTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrDeviceTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrDeviceTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrDeviceTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrDeviceTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2))

	return &HrDeviceTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// HrProcessorFrwID is the column hrProcessorFrwID of table hrProcessorTable.
// The product ID of the firmware associated with the processor.
var HrProcessorFrwID = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 3, 1, 1), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// HrProcessorLoad is the column hrProcessorLoad of table hrProcessorTable.
// The average, over the last minute, of the percentage of time that this
// processor was not idle. Implementations may approximate this one minute
// smoothing period if necessary.
var HrProcessorLoad = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 3, 1, 2), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrProcessorTableRow is one row of hrProcessorTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrProcessorTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrProcessorTableRow struct {
	Index            snmp.OID
	HrProcessorFrwID snmp.OID
	HrProcessorLoad  int32

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrProcessorTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrProcessorFrwID.Key():
		return r.observed[0]&(1<<0) != 0
	case HrProcessorLoad.Key():
		return r.observed[0]&(1<<1) != 0
	}

	return false
}

// HrProcessorTableWalker is a table-aware walker over hrProcessorTable.
// Construct via HrProcessorTable.Walk(ctx, sess, cols...).
type HrProcessorTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrProcessorTableWalker) Iter() iter.Seq2[snmp.OID, HrProcessorTableRow] {
	return func(yield func(snmp.OID, HrProcessorTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 3, 1).WireBytes()
		buffer := make(map[string]*HrProcessorTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrProcessorTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrProcessorFrwID.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrProcessorFrwID = dv
						row.observed[0] |= 1 << 0
					}
				}
			case 2:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrProcessorLoad = int32(v)
					row.observed[0] |= 1 << 1
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrProcessorLoad.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrProcessorLoad = dv
							row.observed[0] |= 1 << 1
						}
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrProcessorTableWalker) Err() error {
	return tw.rw.Err()
}

// hrProcessorTableT is the singleton type of HrProcessorTable.
type hrProcessorTableT struct{}

// HrProcessorTable is the descriptor for the hrProcessorTable table.
var HrProcessorTable hrProcessorTableT

// Walk launches a BulkWalk over hrProcessorTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrProcessorTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrProcessorTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrProcessorTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 3, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrProcessorTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrProcessorTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 3))

	return &HrProcessorTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// HrNetworkIfIndex is the column hrNetworkIfIndex of table hrNetworkTable.
// The value of ifIndex which corresponds to this network device. If this
// device is not represented in the ifTable, then this value shall be zero.
var HrNetworkIfIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 4, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrNetworkTableRow is one row of hrNetworkTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrNetworkTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrNetworkTableRow struct {
	Index            snmp.OID
	HrNetworkIfIndex int32

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrNetworkTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrNetworkIfIndex.Key():
		return r.observed[0]&(1<<0) != 0
	}

	return false
}

// HrNetworkTableWalker is a table-aware walker over hrNetworkTable.
// Construct via HrNetworkTable.Walk(ctx, sess, cols...).
type HrNetworkTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrNetworkTableWalker) Iter() iter.Seq2[snmp.OID, HrNetworkTableRow] {
	return func(yield func(snmp.OID, HrNetworkTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 4, 1).WireBytes()
		buffer := make(map[string]*HrNetworkTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrNetworkTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrNetworkIfIndex = int32(v)
					row.observed[0] |= 1 << 0
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrNetworkIfIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrNetworkIfIndex = dv
							row.observed[0] |= 1 << 0
						}
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrNetworkTableWalker) Err() error {
	return tw.rw.Err()
}

// hrNetworkTableT is the singleton type of HrNetworkTable.
type hrNetworkTableT struct{}

// HrNetworkTable is the descriptor for the hrNetworkTable table.
var HrNetworkTable hrNetworkTableT

// Walk launches a BulkWalk over hrNetworkTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrNetworkTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrNetworkTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrNetworkTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 4, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrNetworkTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrNetworkTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 4))

	return &HrNetworkTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// HrPrinterStatus is the column hrPrinterStatus of table hrPrinterTable.
// The current status of this printer device.
var HrPrinterStatus = snmp.NewColumn[HrPrinterStatusValue](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 5, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (HrPrinterStatusValue, error) {
	v, err := snmp.DecodeInt32(vb)
	if err != nil {
		return HrPrinterStatusValue(0), err
	}
	return HrPrinterStatusValue(v), nil
})

// HrPrinterDetectedErrorState is the column hrPrinterDetectedErrorState of table hrPrinterTable.
// This object represents any error conditions detected by the printer. The
// error conditions are encoded as bits in an octet string, with the
// following definitions: Condition Bit # lowPaper 0 noPaper 1 lowToner 2
// noToner 3 doorOpen 4 jammed 5 offline 6 serviceRequested 7
// inputTrayMissing 8 outputTrayMissing 9 markerSupplyMissing 10
// outputNearFull 11 outputFull 12 inputTrayEmpty 13 overduePreventMaint 14
// Bits are numbered starting with the most significant bit of the first
// byte being bit 0, the least significant bit of the first byte being bit
// 7, the most significant bit of the second byte being bit 8, and so on. A
// one bit encodes that the condition was detected, while a zero bit
// encodes that the condition was not detected. This object is useful for
// alerting an operator to specific warning or error conditions that may
// occur, especially those requiring human intervention.
var HrPrinterDetectedErrorState = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 5, 1, 2), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// HrPrinterTableRow is one row of hrPrinterTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrPrinterTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrPrinterTableRow struct {
	Index                       snmp.OID
	HrPrinterStatus             HrPrinterStatusValue
	HrPrinterDetectedErrorState []byte

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrPrinterTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrPrinterStatus.Key():
		return r.observed[0]&(1<<0) != 0
	case HrPrinterDetectedErrorState.Key():
		return r.observed[0]&(1<<1) != 0
	}

	return false
}

// HrPrinterTableWalker is a table-aware walker over hrPrinterTable.
// Construct via HrPrinterTable.Walk(ctx, sess, cols...).
type HrPrinterTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrPrinterTableWalker) Iter() iter.Seq2[snmp.OID, HrPrinterTableRow] {
	return func(yield func(snmp.OID, HrPrinterTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 5, 1).WireBytes()
		buffer := make(map[string]*HrPrinterTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrPrinterTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrPrinterStatus = HrPrinterStatusValue(v)
					row.observed[0] |= 1 << 0
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrPrinterStatus.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrPrinterStatus = dv
							row.observed[0] |= 1 << 0
						}
					}
				}
			case 2:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrPrinterDetectedErrorState.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrPrinterDetectedErrorState = dv
						row.observed[0] |= 1 << 1
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrPrinterTableWalker) Err() error {
	return tw.rw.Err()
}

// hrPrinterTableT is the singleton type of HrPrinterTable.
type hrPrinterTableT struct{}

// HrPrinterTable is the descriptor for the hrPrinterTable table.
var HrPrinterTable hrPrinterTableT

// Walk launches a BulkWalk over hrPrinterTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrPrinterTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrPrinterTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrPrinterTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 5, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrPrinterTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrPrinterTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 5))

	return &HrPrinterTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// HrDiskStorageAccess is the column hrDiskStorageAccess of table hrDiskStorageTable.
// An indication if this long-term storage device is readable and writable
// or only readable. This should reflect the media type, any write-protect
// mechanism, and any device configuration that affects the entire device.
var HrDiskStorageAccess = snmp.NewColumn[HrDiskStorageAccessValue](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (HrDiskStorageAccessValue, error) {
	v, err := snmp.DecodeInt32(vb)
	if err != nil {
		return HrDiskStorageAccessValue(0), err
	}
	return HrDiskStorageAccessValue(v), nil
})

// HrDiskStorageMedia is the column hrDiskStorageMedia of table hrDiskStorageTable.
// An indication of the type of media used in this long- term storage
// device.
var HrDiskStorageMedia = snmp.NewColumn[HrDiskStorageMediaValue](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 2), snmp.KindInteger32, func(vb snmp.VarBind) (HrDiskStorageMediaValue, error) {
	v, err := snmp.DecodeInt32(vb)
	if err != nil {
		return HrDiskStorageMediaValue(0), err
	}
	return HrDiskStorageMediaValue(v), nil
})

// HrDiskStorageRemoveble is the column hrDiskStorageRemoveble of table hrDiskStorageTable.
// Denotes whether or not the disk media may be removed from the drive.
var HrDiskStorageRemoveble = snmp.NewColumn[bool](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 3), snmp.KindInteger32, func(vb snmp.VarBind) (bool, error) {
	return snmp.DecodeTruthValue(vb)
})

// HrDiskStorageCapacity is the column hrDiskStorageCapacity of table hrDiskStorageTable.
// The total size for this long-term storage device. If the media is
// removable and is currently removed, this value should be zero.
var HrDiskStorageCapacity = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 4), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrDiskStorageTableRow is one row of hrDiskStorageTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrDiskStorageTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrDiskStorageTableRow struct {
	Index                  snmp.OID
	HrDiskStorageAccess    HrDiskStorageAccessValue
	HrDiskStorageMedia     HrDiskStorageMediaValue
	HrDiskStorageRemoveble bool
	HrDiskStorageCapacity  int32

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrDiskStorageTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrDiskStorageAccess.Key():
		return r.observed[0]&(1<<0) != 0
	case HrDiskStorageMedia.Key():
		return r.observed[0]&(1<<1) != 0
	case HrDiskStorageRemoveble.Key():
		return r.observed[0]&(1<<2) != 0
	case HrDiskStorageCapacity.Key():
		return r.observed[0]&(1<<3) != 0
	}

	return false
}

// HrDiskStorageTableWalker is a table-aware walker over hrDiskStorageTable.
// Construct via HrDiskStorageTable.Walk(ctx, sess, cols...).
type HrDiskStorageTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrDiskStorageTableWalker) Iter() iter.Seq2[snmp.OID, HrDiskStorageTableRow] {
	return func(yield func(snmp.OID, HrDiskStorageTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1).WireBytes()
		buffer := make(map[string]*HrDiskStorageTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrDiskStorageTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrDiskStorageAccess = HrDiskStorageAccessValue(v)
					row.observed[0] |= 1 << 0
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrDiskStorageAccess.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrDiskStorageAccess = dv
							row.observed[0] |= 1 << 0
						}
					}
				}
			case 2:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrDiskStorageMedia = HrDiskStorageMediaValue(v)
					row.observed[0] |= 1 << 1
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrDiskStorageMedia.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrDiskStorageMedia = dv
							row.observed[0] |= 1 << 1
						}
					}
				}
			case 3:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrDiskStorageRemoveble.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrDiskStorageRemoveble = dv
						row.observed[0] |= 1 << 2
					}
				}
			case 4:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrDiskStorageCapacity = int32(v)
					row.observed[0] |= 1 << 3
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrDiskStorageCapacity.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrDiskStorageCapacity = dv
							row.observed[0] |= 1 << 3
						}
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrDiskStorageTableWalker) Err() error {
	return tw.rw.Err()
}

// hrDiskStorageTableT is the singleton type of HrDiskStorageTable.
type hrDiskStorageTableT struct{}

// HrDiskStorageTable is the descriptor for the hrDiskStorageTable table.
var HrDiskStorageTable hrDiskStorageTableT

// Walk launches a BulkWalk over hrDiskStorageTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrDiskStorageTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrDiskStorageTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrDiskStorageTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrDiskStorageTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrDiskStorageTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6))

	return &HrDiskStorageTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// HrPartitionIndex is the column hrPartitionIndex of table hrPartitionTable.
// A unique value for each partition on this long-term storage device. The
// value for each long-term storage device must remain constant at least
// from one re- initialization of the agent to the next re- initialization.
var HrPartitionIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrPartitionLabel is the column hrPartitionLabel of table hrPartitionTable.
// A textual description of this partition.
var HrPartitionLabel = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 2), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// HrPartitionID is the column hrPartitionID of table hrPartitionTable.
// A descriptor which uniquely represents this partition to the responsible
// operating system. On some systems, this might take on a binary
// representation.
var HrPartitionID = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 3), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// HrPartitionSize is the column hrPartitionSize of table hrPartitionTable.
// The size of this partition.
var HrPartitionSize = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 4), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrPartitionFSIndex is the column hrPartitionFSIndex of table hrPartitionTable.
// The index of the file system mounted on this partition. If no file
// system is mounted on this partition, then this value shall be zero. Note
// that multiple partitions may point to one file system, denoting that
// that file system resides on those partitions. Multiple file systems may
// not reside on one partition.
var HrPartitionFSIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 5), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrPartitionTableRow is one row of hrPartitionTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrPartitionTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrPartitionTableRow struct {
	Index              snmp.OID
	HrPartitionIndex   int32
	HrPartitionLabel   []byte
	HrPartitionID      []byte
	HrPartitionSize    int32
	HrPartitionFSIndex int32

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrPartitionTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrPartitionIndex.Key():
		return r.observed[0]&(1<<0) != 0
	case HrPartitionLabel.Key():
		return r.observed[0]&(1<<1) != 0
	case HrPartitionID.Key():
		return r.observed[0]&(1<<2) != 0
	case HrPartitionSize.Key():
		return r.observed[0]&(1<<3) != 0
	case HrPartitionFSIndex.Key():
		return r.observed[0]&(1<<4) != 0
	}

	return false
}

// HrPartitionTableWalker is a table-aware walker over hrPartitionTable.
// Construct via HrPartitionTable.Walk(ctx, sess, cols...).
type HrPartitionTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrPartitionTableWalker) Iter() iter.Seq2[snmp.OID, HrPartitionTableRow] {
	return func(yield func(snmp.OID, HrPartitionTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1).WireBytes()
		buffer := make(map[string]*HrPartitionTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrPartitionTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrPartitionIndex = int32(v)
					row.observed[0] |= 1 << 0
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrPartitionIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrPartitionIndex = dv
							row.observed[0] |= 1 << 0
						}
					}
				}
			case 2:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrPartitionLabel.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrPartitionLabel = dv
						row.observed[0] |= 1 << 1
					}
				}
			case 3:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrPartitionID.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrPartitionID = dv
						row.observed[0] |= 1 << 2
					}
				}
			case 4:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrPartitionSize = int32(v)
					row.observed[0] |= 1 << 3
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrPartitionSize.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrPartitionSize = dv
							row.observed[0] |= 1 << 3
						}
					}
				}
			case 5:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrPartitionFSIndex = int32(v)
					row.observed[0] |= 1 << 4
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrPartitionFSIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrPartitionFSIndex = dv
							row.observed[0] |= 1 << 4
						}
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrPartitionTableWalker) Err() error {
	return tw.rw.Err()
}

// hrPartitionTableT is the singleton type of HrPartitionTable.
type hrPartitionTableT struct{}

// HrPartitionTable is the descriptor for the hrPartitionTable table.
var HrPartitionTable hrPartitionTableT

// Walk launches a BulkWalk over hrPartitionTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrPartitionTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrPartitionTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrPartitionTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrPartitionTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrPartitionTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7))

	return &HrPartitionTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// HrFSIndex is the column hrFSIndex of table hrFSTable.
// A unique value for each file system local to this host. The value for
// each file system must remain constant at least from one
// re-initialization of the agent to the next re-initialization.
var HrFSIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrFSMountPoint is the column hrFSMountPoint of table hrFSTable.
// The path name of the root of this file system.
var HrFSMountPoint = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 2), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// HrFSRemoteMountPoint is the column hrFSRemoteMountPoint of table hrFSTable.
// A description of the name and/or address of the server that this file
// system is mounted from. This may also include parameters such as the
// mount point on the remote file system. If this is not a remote file
// system, this string should have a length of zero.
var HrFSRemoteMountPoint = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 3), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// HrFSType is the column hrFSType of table hrFSTable.
// The value of this object identifies the type of this file system.
var HrFSType = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 4), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// HrFSAccess is the column hrFSAccess of table hrFSTable.
// An indication if this file system is logically configured by the
// operating system to be readable and writable or only readable. This does
// not represent any local access-control policy, except one that is
// applied to the file system as a whole.
var HrFSAccess = snmp.NewColumn[HrFSAccessValue](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 5), snmp.KindInteger32, func(vb snmp.VarBind) (HrFSAccessValue, error) {
	v, err := snmp.DecodeInt32(vb)
	if err != nil {
		return HrFSAccessValue(0), err
	}
	return HrFSAccessValue(v), nil
})

// HrFSBootable is the column hrFSBootable of table hrFSTable.
// A flag indicating whether this file system is bootable.
var HrFSBootable = snmp.NewColumn[bool](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 6), snmp.KindInteger32, func(vb snmp.VarBind) (bool, error) {
	return snmp.DecodeTruthValue(vb)
})

// HrFSStorageIndex is the column hrFSStorageIndex of table hrFSTable.
// The index of the hrStorageEntry that represents information about this
// file system. If there is no such information available, then this value
// shall be zero. The relevant storage entry will be useful in tracking the
// percent usage of this file system and diagnosing errors that may occur
// when it runs out of space.
var HrFSStorageIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 7), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrFSLastFullBackupDate is the column hrFSLastFullBackupDate of table hrFSTable.
// The last date at which this complete file system was copied to another
// storage device for backup. This information is useful for ensuring that
// backups are being performed regularly. If this information is not known,
// then this variable shall have the value corresponding to January 1, year
// 0000, 00:00:00.0, which is encoded as (hex)'00 00 01 01 00 00 00 00'.
var HrFSLastFullBackupDate = snmp.NewColumn[time.Time](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 8), snmp.KindOctetString, func(vb snmp.VarBind) (time.Time, error) {
	return snmp.DecodeDateAndTime(vb)
})

// HrFSLastPartialBackupDate is the column hrFSLastPartialBackupDate of table hrFSTable.
// The last date at which a portion of this file system was copied to
// another storage device for backup. This information is useful for
// ensuring that backups are being performed regularly. If this information
// is not known, then this variable shall have the value corresponding to
// January 1, year 0000, 00:00:00.0, which is encoded as (hex)'00 00 01 01
// 00 00 00 00'.
var HrFSLastPartialBackupDate = snmp.NewColumn[time.Time](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 9), snmp.KindOctetString, func(vb snmp.VarBind) (time.Time, error) {
	return snmp.DecodeDateAndTime(vb)
})

// HrFSTableRow is one row of hrFSTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrFSTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrFSTableRow struct {
	Index                     snmp.OID
	HrFSIndex                 int32
	HrFSMountPoint            []byte
	HrFSRemoteMountPoint      []byte
	HrFSType                  snmp.OID
	HrFSAccess                HrFSAccessValue
	HrFSBootable              bool
	HrFSStorageIndex          int32
	HrFSLastFullBackupDate    time.Time
	HrFSLastPartialBackupDate time.Time

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrFSTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrFSIndex.Key():
		return r.observed[0]&(1<<0) != 0
	case HrFSMountPoint.Key():
		return r.observed[0]&(1<<1) != 0
	case HrFSRemoteMountPoint.Key():
		return r.observed[0]&(1<<2) != 0
	case HrFSType.Key():
		return r.observed[0]&(1<<3) != 0
	case HrFSAccess.Key():
		return r.observed[0]&(1<<4) != 0
	case HrFSBootable.Key():
		return r.observed[0]&(1<<5) != 0
	case HrFSStorageIndex.Key():
		return r.observed[0]&(1<<6) != 0
	case HrFSLastFullBackupDate.Key():
		return r.observed[0]&(1<<7) != 0
	case HrFSLastPartialBackupDate.Key():
		return r.observed[0]&(1<<8) != 0
	}

	return false
}

// HrFSTableWalker is a table-aware walker over hrFSTable.
// Construct via HrFSTable.Walk(ctx, sess, cols...).
type HrFSTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrFSTableWalker) Iter() iter.Seq2[snmp.OID, HrFSTableRow] {
	return func(yield func(snmp.OID, HrFSTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1).WireBytes()
		buffer := make(map[string]*HrFSTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrFSTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrFSIndex = int32(v)
					row.observed[0] |= 1 << 0
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrFSIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrFSIndex = dv
							row.observed[0] |= 1 << 0
						}
					}
				}
			case 2:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrFSMountPoint.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrFSMountPoint = dv
						row.observed[0] |= 1 << 1
					}
				}
			case 3:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrFSRemoteMountPoint.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrFSRemoteMountPoint = dv
						row.observed[0] |= 1 << 2
					}
				}
			case 4:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrFSType.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrFSType = dv
						row.observed[0] |= 1 << 3
					}
				}
			case 5:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrFSAccess = HrFSAccessValue(v)
					row.observed[0] |= 1 << 4
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrFSAccess.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrFSAccess = dv
							row.observed[0] |= 1 << 4
						}
					}
				}
			case 6:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrFSBootable.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrFSBootable = dv
						row.observed[0] |= 1 << 5
					}
				}
			case 7:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrFSStorageIndex = int32(v)
					row.observed[0] |= 1 << 6
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrFSStorageIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrFSStorageIndex = dv
							row.observed[0] |= 1 << 6
						}
					}
				}
			case 8:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrFSLastFullBackupDate.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrFSLastFullBackupDate = dv
						row.observed[0] |= 1 << 7
					}
				}
			case 9:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrFSLastPartialBackupDate.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrFSLastPartialBackupDate = dv
						row.observed[0] |= 1 << 8
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrFSTableWalker) Err() error {
	return tw.rw.Err()
}

// hrFSTableT is the singleton type of HrFSTable.
type hrFSTableT struct{}

// HrFSTable is the descriptor for the hrFSTable table.
var HrFSTable hrFSTableT

// Walk launches a BulkWalk over hrFSTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrFSTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrFSTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrFSTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrFSTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrFSTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8))

	return &HrFSTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// HrSWRunIndex is the column hrSWRunIndex of table hrSWRunTable.
// A unique value for each piece of software running on the host. Wherever
// possible, this should be the system's native, unique identification
// number.
var HrSWRunIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrSWRunName is the column hrSWRunName of table hrSWRunTable.
// A textual description of this running piece of software, including the
// manufacturer, revision, and the name by which it is commonly known. If
// this software was installed locally, this should be the same string as
// used in the corresponding hrSWInstalledName.
var HrSWRunName = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 2), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// HrSWRunID is the column hrSWRunID of table hrSWRunTable.
// The product ID of this running piece of software.
var HrSWRunID = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 3), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// HrSWRunPath is the column hrSWRunPath of table hrSWRunTable.
// A description of the location on long-term storage (e.g. a disk drive)
// from which this software was loaded.
var HrSWRunPath = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 4), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// HrSWRunParameters is the column hrSWRunParameters of table hrSWRunTable.
// A description of the parameters supplied to this software when it was
// initially loaded.
var HrSWRunParameters = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 5), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// HrSWRunType is the column hrSWRunType of table hrSWRunTable.
// The type of this software.
var HrSWRunType = snmp.NewColumn[HrSWRunTypeValue](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 6), snmp.KindInteger32, func(vb snmp.VarBind) (HrSWRunTypeValue, error) {
	v, err := snmp.DecodeInt32(vb)
	if err != nil {
		return HrSWRunTypeValue(0), err
	}
	return HrSWRunTypeValue(v), nil
})

// HrSWRunStatus is the column hrSWRunStatus of table hrSWRunTable.
// The status of this running piece of software. Setting this value to
// invalid(4) shall cause this software to stop running and to be unloaded.
// Sets to other values are not valid.
var HrSWRunStatus = snmp.NewColumn[HrSWRunStatusValue](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 7), snmp.KindInteger32, func(vb snmp.VarBind) (HrSWRunStatusValue, error) {
	v, err := snmp.DecodeInt32(vb)
	if err != nil {
		return HrSWRunStatusValue(0), err
	}
	return HrSWRunStatusValue(v), nil
})

// HrSWRunTableRow is one row of hrSWRunTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrSWRunTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrSWRunTableRow struct {
	Index             snmp.OID
	HrSWRunIndex      int32
	HrSWRunName       []byte
	HrSWRunID         snmp.OID
	HrSWRunPath       []byte
	HrSWRunParameters []byte
	HrSWRunType       HrSWRunTypeValue
	HrSWRunStatus     HrSWRunStatusValue

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrSWRunTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrSWRunIndex.Key():
		return r.observed[0]&(1<<0) != 0
	case HrSWRunName.Key():
		return r.observed[0]&(1<<1) != 0
	case HrSWRunID.Key():
		return r.observed[0]&(1<<2) != 0
	case HrSWRunPath.Key():
		return r.observed[0]&(1<<3) != 0
	case HrSWRunParameters.Key():
		return r.observed[0]&(1<<4) != 0
	case HrSWRunType.Key():
		return r.observed[0]&(1<<5) != 0
	case HrSWRunStatus.Key():
		return r.observed[0]&(1<<6) != 0
	}

	return false
}

// HrSWRunTableWalker is a table-aware walker over hrSWRunTable.
// Construct via HrSWRunTable.Walk(ctx, sess, cols...).
type HrSWRunTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrSWRunTableWalker) Iter() iter.Seq2[snmp.OID, HrSWRunTableRow] {
	return func(yield func(snmp.OID, HrSWRunTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1).WireBytes()
		buffer := make(map[string]*HrSWRunTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrSWRunTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrSWRunIndex = int32(v)
					row.observed[0] |= 1 << 0
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrSWRunIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrSWRunIndex = dv
							row.observed[0] |= 1 << 0
						}
					}
				}
			case 2:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrSWRunName.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrSWRunName = dv
						row.observed[0] |= 1 << 1
					}
				}
			case 3:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrSWRunID.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrSWRunID = dv
						row.observed[0] |= 1 << 2
					}
				}
			case 4:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrSWRunPath.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrSWRunPath = dv
						row.observed[0] |= 1 << 3
					}
				}
			case 5:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrSWRunParameters.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrSWRunParameters = dv
						row.observed[0] |= 1 << 4
					}
				}
			case 6:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrSWRunType = HrSWRunTypeValue(v)
					row.observed[0] |= 1 << 5
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrSWRunType.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrSWRunType = dv
							row.observed[0] |= 1 << 5
						}
					}
				}
			case 7:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrSWRunStatus = HrSWRunStatusValue(v)
					row.observed[0] |= 1 << 6
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrSWRunStatus.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrSWRunStatus = dv
							row.observed[0] |= 1 << 6
						}
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrSWRunTableWalker) Err() error {
	return tw.rw.Err()
}

// hrSWRunTableT is the singleton type of HrSWRunTable.
type hrSWRunTableT struct{}

// HrSWRunTable is the descriptor for the hrSWRunTable table.
var HrSWRunTable hrSWRunTableT

// Walk launches a BulkWalk over hrSWRunTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrSWRunTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrSWRunTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrSWRunTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrSWRunTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrSWRunTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2))

	return &HrSWRunTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// HrSWRunPerfCPU is the column hrSWRunPerfCPU of table hrSWRunPerfTable.
// The number of centi-seconds of the total system's CPU resources consumed
// by this process. Note that on a multi-processor system, this value may
// increment by more than one centi-second in one centi-second of real
// (wall clock) time.
var HrSWRunPerfCPU = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 5, 1, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrSWRunPerfMem is the column hrSWRunPerfMem of table hrSWRunPerfTable.
// The total amount of real system memory allocated to this process.
var HrSWRunPerfMem = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 5, 1, 1, 2), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrSWRunPerfTableRow is one row of hrSWRunPerfTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrSWRunPerfTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrSWRunPerfTableRow struct {
	Index          snmp.OID
	HrSWRunPerfCPU int32
	HrSWRunPerfMem int32

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrSWRunPerfTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrSWRunPerfCPU.Key():
		return r.observed[0]&(1<<0) != 0
	case HrSWRunPerfMem.Key():
		return r.observed[0]&(1<<1) != 0
	}

	return false
}

// HrSWRunPerfTableWalker is a table-aware walker over hrSWRunPerfTable.
// Construct via HrSWRunPerfTable.Walk(ctx, sess, cols...).
type HrSWRunPerfTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrSWRunPerfTableWalker) Iter() iter.Seq2[snmp.OID, HrSWRunPerfTableRow] {
	return func(yield func(snmp.OID, HrSWRunPerfTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 5, 1, 1).WireBytes()
		buffer := make(map[string]*HrSWRunPerfTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrSWRunPerfTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrSWRunPerfCPU = int32(v)
					row.observed[0] |= 1 << 0
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrSWRunPerfCPU.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrSWRunPerfCPU = dv
							row.observed[0] |= 1 << 0
						}
					}
				}
			case 2:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrSWRunPerfMem = int32(v)
					row.observed[0] |= 1 << 1
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrSWRunPerfMem.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrSWRunPerfMem = dv
							row.observed[0] |= 1 << 1
						}
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrSWRunPerfTableWalker) Err() error {
	return tw.rw.Err()
}

// hrSWRunPerfTableT is the singleton type of HrSWRunPerfTable.
type hrSWRunPerfTableT struct{}

// HrSWRunPerfTable is the descriptor for the hrSWRunPerfTable table.
var HrSWRunPerfTable hrSWRunPerfTableT

// Walk launches a BulkWalk over hrSWRunPerfTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrSWRunPerfTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrSWRunPerfTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrSWRunPerfTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 5, 1, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrSWRunPerfTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrSWRunPerfTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 5, 1))

	return &HrSWRunPerfTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// HrSWInstalledIndex is the column hrSWInstalledIndex of table hrSWInstalledTable.
// A unique value for each piece of software installed on the host. This
// value shall be in the range from 1 to the number of pieces of software
// installed on the host.
var HrSWInstalledIndex = snmp.NewColumn[int32](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 1), snmp.KindInteger32, func(vb snmp.VarBind) (int32, error) {
	return snmp.DecodeInt32(vb)
})

// HrSWInstalledName is the column hrSWInstalledName of table hrSWInstalledTable.
// A textual description of this installed piece of software, including the
// manufacturer, revision, the name by which it is commonly known, and
// optionally, its serial number.
var HrSWInstalledName = snmp.NewColumn[[]byte](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 2), snmp.KindOctetString, func(vb snmp.VarBind) ([]byte, error) {
	return snmp.DecodeBytes(vb)
})

// HrSWInstalledID is the column hrSWInstalledID of table hrSWInstalledTable.
// The product ID of this installed piece of software.
var HrSWInstalledID = snmp.NewColumn[snmp.OID](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 3), snmp.KindObjectID, func(vb snmp.VarBind) (snmp.OID, error) {
	return snmp.DecodeOID(vb)
})

// HrSWInstalledType is the column hrSWInstalledType of table hrSWInstalledTable.
// The type of this software.
var HrSWInstalledType = snmp.NewColumn[HrSWInstalledTypeValue](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 4), snmp.KindInteger32, func(vb snmp.VarBind) (HrSWInstalledTypeValue, error) {
	v, err := snmp.DecodeInt32(vb)
	if err != nil {
		return HrSWInstalledTypeValue(0), err
	}
	return HrSWInstalledTypeValue(v), nil
})

// HrSWInstalledDate is the column hrSWInstalledDate of table hrSWInstalledTable.
// The last-modification date of this application as it would appear in a
// directory listing. If this information is not known, then this variable
// shall have the value corresponding to January 1, year 0000, 00:00:00.0,
// which is encoded as (hex)'00 00 01 01 00 00 00 00'.
var HrSWInstalledDate = snmp.NewColumn[time.Time](snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 5), snmp.KindOctetString, func(vb snmp.VarBind) (time.Time, error) {
	return snmp.DecodeDateAndTime(vb)
})

// HrSWInstalledTableRow is one row of hrSWInstalledTable. Index carries the OID
// suffix beyond the table-entry prefix; the remaining fields are
// populated only for columns the caller passed to Walk(). Use
// HrSWInstalledTableRow.Observed to tell a reported zero from a column the
// agent never answered.
type HrSWInstalledTableRow struct {
	Index              snmp.OID
	HrSWInstalledIndex int32
	HrSWInstalledName  []byte
	HrSWInstalledID    snmp.OID
	HrSWInstalledType  HrSWInstalledTypeValue
	HrSWInstalledDate  time.Time

	// observed carries one bit per column of this table, in
	// column-OID order, set when the walk decoded a value for
	// that column on this row.
	observed [1]uint64
}

// Observed reports whether col returned a value for this row. A column
// the agent answered reads true even when the answer was zero or empty;
// a column that was requested but never landed, one that was not passed
// to Walk, and any column of another table all read false.
func (r HrSWInstalledTableRow) Observed(col snmp.AnyColumn) bool {
	switch col.Key() {
	case HrSWInstalledIndex.Key():
		return r.observed[0]&(1<<0) != 0
	case HrSWInstalledName.Key():
		return r.observed[0]&(1<<1) != 0
	case HrSWInstalledID.Key():
		return r.observed[0]&(1<<2) != 0
	case HrSWInstalledType.Key():
		return r.observed[0]&(1<<3) != 0
	case HrSWInstalledDate.Key():
		return r.observed[0]&(1<<4) != 0
	}

	return false
}

// HrSWInstalledTableWalker is a table-aware walker over hrSWInstalledTable.
// Construct via HrSWInstalledTable.Walk(ctx, sess, cols...).
type HrSWInstalledTableWalker struct {
	rw    *snmp.RawWalker
	cols  []snmp.AnyColumn
	byCol map[uint32]snmp.AnyColumn
}

// Iter yields one (Index, Row) pair per row of the table walk. The
// full BulkWalk is buffered before any row is yielded, so the
// generated walker is correct over both column-major and row-major
// agent emission. Contracts:
//
//  1. Ordering: rows yield in the index's first-appearance position
//     in the agent's BulkWalk response — which for a well-behaved
//     agent equals lexicographic OID order over the index suffix.
//     This is NOT numerical order for composite-index tables
//     (e.g. ipAddrTable indexed by IP-as-OID: 192.168.0.10 sorts
//     before 192.168.0.2). Integer-keyed tables (ifTable,
//     hrProcessorTable) get numeric order for free.
//
//  2. Row presence: every index observed under the entry prefix
//     yields a row, even when only unrequested columns landed on
//     that index. The row's requested-column fields stay at zero
//     and Observed reports every column of that row as unobserved.
//
//  3. Decode error: rows for indexes strictly before the failing
//     index in appearance order flush before Walker.Fail is set,
//     preserving partial-progress visibility for the operator.
//     The failing row and anything after it are not yielded.
//     Check Err() afterwards for the terminal cause.
//
//  4. Memory profile: O(rows × requested columns) buffered before
//     the first yield. Bounded by table size, not walk position —
//     callers that broke out early via 'for row := range Iter()'
//     still pay the full-walk buffer cost.
func (tw *HrSWInstalledTableWalker) Iter() iter.Seq2[snmp.OID, HrSWInstalledTableRow] {
	return func(yield func(snmp.OID, HrSWInstalledTableRow) bool) {
		entryWire := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1).WireBytes()
		buffer := make(map[string]*HrSWInstalledTableRow)
		var orderIdx []snmp.OID
		var orderKey []string

		for rv := range tw.rw.Iter() {
			if !bytes.HasPrefix(rv.OID, entryWire) {
				continue
			}
			suffix := rv.OID[len(entryWire):]
			colID, colLen, okArc := snmp.RawFirstArc(suffix)
			if !okArc || colLen >= len(suffix) {
				continue
			}
			idxWire := suffix[colLen:]
			row, exists := buffer[string(idxWire)]
			if !exists {
				idx, idxErr := snmp.DecodeIndexArcs(idxWire)
				if idxErr != nil {
					continue
				}
				key := string(idxWire)
				row = &HrSWInstalledTableRow{}
				buffer[key] = row
				orderIdx = append(orderIdx, idx)
				orderKey = append(orderKey, key)
			}
			_, ok := tw.byCol[colID]
			if !ok {
				continue
			}
			var derr error
			switch colID {
			case 1:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrSWInstalledIndex = int32(v)
					row.observed[0] |= 1 << 0
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrSWInstalledIndex.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrSWInstalledIndex = dv
							row.observed[0] |= 1 << 0
						}
					}
				}
			case 2:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrSWInstalledName.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrSWInstalledName = dv
						row.observed[0] |= 1 << 1
					}
				}
			case 3:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrSWInstalledID.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrSWInstalledID = dv
						row.observed[0] |= 1 << 2
					}
				}
			case 4:
				if v, okRaw := snmp.RawInteger32(rv); okRaw {
					row.HrSWInstalledType = HrSWInstalledTypeValue(v)
					row.observed[0] |= 1 << 3
				} else {
					vb, vbErr := rv.Decode()
					if vbErr != nil {
						derr = vbErr
					} else {
						dv, dErr := HrSWInstalledType.Decode(vb)
						if dErr != nil {
							derr = dErr
						} else {
							row.HrSWInstalledType = dv
							row.observed[0] |= 1 << 3
						}
					}
				}
			case 5:
				vb, vbErr := rv.Decode()
				if vbErr != nil {
					derr = vbErr
				} else {
					dv, dErr := HrSWInstalledDate.Decode(vb)
					if dErr != nil {
						derr = dErr
					} else {
						row.HrSWInstalledDate = dv
						row.observed[0] |= 1 << 4
					}
				}
			}
			if derr != nil {
				for i := 0; i < len(orderKey); i++ {
					if orderKey[i] == string(idxWire) {
						break
					}
					if !yield(orderIdx[i], *buffer[orderKey[i]]) {
						tw.rw.Fail(derr)
						return
					}
				}
				tw.rw.Fail(derr)
				return
			}
		}

		for i := 0; i < len(orderIdx); i++ {
			if !yield(orderIdx[i], *buffer[orderKey[i]]) {
				return
			}
		}
	}
}

// Err returns the underlying walker's terminal error, or nil if
// the walk completed naturally.
func (tw *HrSWInstalledTableWalker) Err() error {
	return tw.rw.Err()
}

// hrSWInstalledTableT is the singleton type of HrSWInstalledTable.
type hrSWInstalledTableT struct{}

// HrSWInstalledTable is the descriptor for the hrSWInstalledTable table.
var HrSWInstalledTable hrSWInstalledTableT

// Walk launches a BulkWalk over hrSWInstalledTable and returns a
// table-aware iterator. Only the columns listed in cols are
// decoded; varbinds for unlisted columns are skipped. The walk
// rides the raw fast path (BulkWalkRaw); sessions or responses
// that cannot deliver raw bytes degrade transparently to the
// generic per-varbind decode.
//
// Every column in cols must be a column of hrSWInstalledTable. A column
// of any other table is a caller bug, not a device quirk: no request
// is sent, the iterator yields nothing, and Err reports
// snmp.ErrForeignColumn.
func (hrSWInstalledTableT) Walk(ctx context.Context, sess snmp.Session, cols ...snmp.AnyColumn) *HrSWInstalledTableWalker {
	entry := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1)
	byCol := make(map[uint32]snmp.AnyColumn, len(cols))

	for _, c := range cols {
		o := c.OID()
		if o.Len() != entry.Len()+1 || !o.HasPrefix(entry) {
			return &HrSWInstalledTableWalker{rw: snmp.ForeignColumnWalk(ctx, "hrSWInstalledTable", c)}
		}
		byCol[o.At(o.Len()-1)] = c
	}

	w := sess.BulkWalkRaw(ctx, snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3))

	return &HrSWInstalledTableWalker{
		byCol: byCol,
		cols:  cols,
		rw:    w,
	}
}

// decodeHrSWInstalledTableRow decodes one row of HrSWInstalledTable from the supplied
// VarBinds. Each VarBind's OID determines which row field it populates
// (via the column's last sub-id). VarBinds with unknown column-ids are
// ignored. Absent columns leave their field at its zero value.
func decodeHrSWInstalledTableRow(idx snmp.OID, vbs []snmp.VarBind) (HrSWInstalledTableRow, error) {
	var row HrSWInstalledTableRow
	row.Index = idx

	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 1:
			dv, derr := HrSWInstalledIndex.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.HrSWInstalledIndex = dv
			row.observed[0] |= 1 << 0
		case 2:
			dv, derr := HrSWInstalledName.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.HrSWInstalledName = dv
			row.observed[0] |= 1 << 1
		case 3:
			dv, derr := HrSWInstalledID.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.HrSWInstalledID = dv
			row.observed[0] |= 1 << 2
		case 4:
			dv, derr := HrSWInstalledType.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.HrSWInstalledType = dv
			row.observed[0] |= 1 << 3
		case 5:
			dv, derr := HrSWInstalledDate.Decode(vb)
			if derr != nil {
				return row, derr
			}
			row.HrSWInstalledDate = dv
			row.observed[0] |= 1 << 4
		}
	}

	return row, nil
}

// equalHrSWInstalledTableRow compares two HrSWInstalledTableRow values for equality.
// Used by [snmp.Watcher] to compute ChangeKindModified emits. Field-by-
// field with the type-appropriate comparator (bytes.Equal for []byte,
// OID.Equal for OID, time.Time.Equal for time.Time, == for everything else).
func equalHrSWInstalledTableRow(a HrSWInstalledTableRow, b HrSWInstalledTableRow) bool {
	return a.Index.Equal(b.Index) && a.HrSWInstalledIndex == b.HrSWInstalledIndex && bytes.Equal(a.HrSWInstalledName, b.HrSWInstalledName) && a.HrSWInstalledID.Equal(b.HrSWInstalledID) && a.HrSWInstalledType == b.HrSWInstalledType && a.HrSWInstalledDate.Equal(b.HrSWInstalledDate)
}

// mergeHrSWInstalledTableRow merges the values decoded from vbs into dst, leaving fields
// whose columns are not present in vbs unchanged. Used by [snmp.Watcher]
// to maintain per-row state under partial-column fetches (Counter-tier,
// Static-tier). Best-effort: individual VarBind decode failures are
// silently skipped rather than propagated, because partial-fetch ticks
// surface transient errors through [snmp.Watcher.LastTickErr] at the
// call-site granularity rather than per-VarBind.
func mergeHrSWInstalledTableRow(dst *HrSWInstalledTableRow, vbs []snmp.VarBind) {
	for _, vb := range vbs {
		o := vb.GetHeader().OID
		if o.Len() == 0 {
			continue
		}
		entryLen := snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1).Len()
		if o.Len() <= entryLen {
			continue
		}
		colID := o.At(entryLen)
		switch colID {
		case 1:
			dv, derr := HrSWInstalledIndex.Decode(vb)
			if derr == nil {
				dst.HrSWInstalledIndex = dv
				dst.observed[0] |= 1 << 0
			}
		case 2:
			dv, derr := HrSWInstalledName.Decode(vb)
			if derr == nil {
				dst.HrSWInstalledName = dv
				dst.observed[0] |= 1 << 1
			}
		case 3:
			dv, derr := HrSWInstalledID.Decode(vb)
			if derr == nil {
				dst.HrSWInstalledID = dv
				dst.observed[0] |= 1 << 2
			}
		case 4:
			dv, derr := HrSWInstalledType.Decode(vb)
			if derr == nil {
				dst.HrSWInstalledType = dv
				dst.observed[0] |= 1 << 3
			}
		case 5:
			dv, derr := HrSWInstalledDate.Decode(vb)
			if derr == nil {
				dst.HrSWInstalledDate = dv
				dst.observed[0] |= 1 << 4
			}
		}
	}
}

// HrSWInstalledTableWatcher is a table-aware Watcher over HrSWInstalledTable.
// Construct via HrSWInstalledTable.Watch(ctx, sess, cols, opts...).
type HrSWInstalledTableWatcher struct {
	w *snmp.Watcher[HrSWInstalledTableRow]
}

// Iter returns the range-over-func view of the Watcher's event stream.
// See [snmp.Watcher.Iter] for the contract.
func (tw *HrSWInstalledTableWatcher) Iter() iter.Seq2[snmp.OID, snmp.WatchEvent[HrSWInstalledTableRow]] {
	return tw.w.Iter()
}

// Err returns the underlying Watcher's terminal error, or nil if it
// completed naturally. See [snmp.Watcher.Err].
func (tw *HrSWInstalledTableWatcher) Err() error {
	return tw.w.Err()
}

// Close signals the Watcher's tick goroutine to terminate. Idempotent.
// See [snmp.Watcher.Close].
func (tw *HrSWInstalledTableWatcher) Close() error {
	return tw.w.Close()
}

// Fallback reports whether the Watcher has transitioned to fallback
// mode. See [snmp.Watcher.Fallback].
func (tw *HrSWInstalledTableWatcher) Fallback() bool {
	return tw.w.Fallback()
}

// LastTickErr returns the most-recent transient per-tick error.
// See [snmp.Watcher.LastTickErr].
func (tw *HrSWInstalledTableWatcher) LastTickErr() error {
	return tw.w.LastTickErr()
}

// TableRoot returns the OID of the table this Watcher operates over.
func (tw *HrSWInstalledTableWatcher) TableRoot() snmp.OID {
	return tw.w.TableRoot()
}

// Watch opens a long-lived watch on HrSWInstalledTable.
//
// cols selects the columns whose values are reported on every emitted
// event. opts override cadence bounds, per-column tier classifications,
// fallback behavior, and other policy — see [snmp.WatchOption] for the
// full set.
//
// cols is a slice rather than variadic because opts is variadic and
// Go forbids two variadic parameters.
//
// Events emitted on this stream:
//   - ChangeKindAdded    — Row populated, Prev always nil.
//   - ChangeKindModified — Row is current state. Prev is nil unless
//     the caller passed [snmp.WithPrevRow]; when
//     set, Prev carries the previous Row.
//   - ChangeKindRemoved  — Row carries the last-known state at the
//     time the row disappeared. Prev is nil.
//
// The returned Watcher must be Closed when the caller is done; iteration
// exit alone does not free the underlying goroutine until Close.
//
// On validation failure (invalid cadence bounds, tier override of the
// indicator column, etc.) Watch returns a Watcher whose Err() returns
// the cause immediately; range loops exit without emitting events and
// Close is a no-op.
func (hrSWInstalledTableT) Watch(ctx context.Context, sess snmp.Session, cols []snmp.AnyColumn, opts ...snmp.WatchOption) *HrSWInstalledTableWatcher {
	allOpts := append([]snmp.WatchOption{snmp.WithTierLookup(ColumnTier)}, opts...)
	w, _ := snmp.NewWatcher[HrSWInstalledTableRow](ctx, sess, HrSWInstalledTableIndicator, cols, decodeHrSWInstalledTableRow, equalHrSWInstalledTableRow, mergeHrSWInstalledTableRow, allOpts...)

	return &HrSWInstalledTableWatcher{w: w}
}

// hOSTRESOURCESMIBOIDDispatch maps column wire keys ([snmp.OID.WireKey]) to their
// typed AnyColumn for fast
// lookup during table-walk decoding. Per-MIB-module — no global
// registry; cross-package callers should consult OIDDispatch().
var hOSTRESOURCESMIBOIDDispatch = map[string]snmp.AnyColumn{
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 1).WireKey(): HrStorageIndex,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 2).WireKey(): HrStorageType,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 3).WireKey(): HrStorageDescr,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 4).WireKey(): HrStorageAllocationUnits,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 5).WireKey(): HrStorageSize,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 6).WireKey(): HrStorageUsed,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 7).WireKey(): HrStorageAllocationFailures,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 1).WireKey(): HrDeviceIndex,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 2).WireKey(): HrDeviceType,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 3).WireKey(): HrDeviceDescr,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 4).WireKey(): HrDeviceID,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 5).WireKey(): HrDeviceStatus,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 6).WireKey(): HrDeviceErrors,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 3, 1, 1).WireKey(): HrProcessorFrwID,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 3, 1, 2).WireKey(): HrProcessorLoad,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 4, 1, 1).WireKey(): HrNetworkIfIndex,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 5, 1, 1).WireKey(): HrPrinterStatus,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 5, 1, 2).WireKey(): HrPrinterDetectedErrorState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 1).WireKey(): HrDiskStorageAccess,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 2).WireKey(): HrDiskStorageMedia,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 3).WireKey(): HrDiskStorageRemoveble,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 4).WireKey(): HrDiskStorageCapacity,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 1).WireKey(): HrPartitionIndex,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 2).WireKey(): HrPartitionLabel,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 3).WireKey(): HrPartitionID,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 4).WireKey(): HrPartitionSize,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 5).WireKey(): HrPartitionFSIndex,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 1).WireKey(): HrFSIndex,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 2).WireKey(): HrFSMountPoint,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 3).WireKey(): HrFSRemoteMountPoint,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 4).WireKey(): HrFSType,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 5).WireKey(): HrFSAccess,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 6).WireKey(): HrFSBootable,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 7).WireKey(): HrFSStorageIndex,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 8).WireKey(): HrFSLastFullBackupDate,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 9).WireKey(): HrFSLastPartialBackupDate,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 1).WireKey(): HrSWRunIndex,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 2).WireKey(): HrSWRunName,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 3).WireKey(): HrSWRunID,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 4).WireKey(): HrSWRunPath,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 5).WireKey(): HrSWRunParameters,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 6).WireKey(): HrSWRunType,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 7).WireKey(): HrSWRunStatus,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 5, 1, 1, 1).WireKey(): HrSWRunPerfCPU,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 5, 1, 1, 2).WireKey(): HrSWRunPerfMem,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 1).WireKey(): HrSWInstalledIndex,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 2).WireKey(): HrSWInstalledName,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 3).WireKey(): HrSWInstalledID,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 4).WireKey(): HrSWInstalledType,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 5).WireKey(): HrSWInstalledDate,
}

// OIDDispatch returns a shallow copy of the package's wire-key →
// AnyColumn map. The copy isolates callers from accidental mutation
// of the package-internal map.
func OIDDispatch() map[string]snmp.AnyColumn {
	out := make(map[string]snmp.AnyColumn, len(hOSTRESOURCESMIBOIDDispatch))
	for k, v := range hOSTRESOURCESMIBOIDDispatch {
		out[k] = v
	}
	return out
}

// hOSTRESOURCESMIBColumnTiers maps column wire keys ([snmp.OID.WireKey]) to their
// snmp.Tier classification.
// Tier classification drives the Watcher's scheduler — counters fire
// on their own cadence, state columns refresh on indicator advance,
// and indicator columns drive the change-detection probe itself.
// See snmp.Tier and snmp.Watcher for the consumption contract.
var hOSTRESOURCESMIBColumnTiers = map[string]snmp.Tier{
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 2).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 3).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 4).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 5).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 6).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 2, 3, 1, 7).WireKey(): snmp.TierCounter,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 2).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 3).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 4).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 5).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 2, 1, 6).WireKey(): snmp.TierCounter,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 3, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 3, 1, 2).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 4, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 5, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 5, 1, 2).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 2).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 3).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 6, 1, 4).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 2).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 3).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 4).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 7, 1, 5).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 2).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 3).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 4).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 5).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 6).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 7).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 8).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 3, 8, 1, 9).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 2).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 3).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 4).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 5).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 6).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 4, 2, 1, 7).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 5, 1, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 5, 1, 1, 2).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 1).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 2).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 3).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 4).WireKey(): snmp.TierState,
	snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3, 1, 5).WireKey(): snmp.TierState,
}

// ColumnTier returns the codegen-classified [snmp.Tier] for col, or
// snmp.TierUnknown when col's OID is not known to this package. The
// generated Watch methods install this function via
// [snmp.WithTierLookup]; returning TierUnknown lets the runtime fall
// back to its wire-Kind heuristic for columns this package does not
// classify. Caller overrides via [snmp.WithColumnTier] still win.
func ColumnTier(col snmp.AnyColumn) snmp.Tier {
	if col == nil {
		return snmp.TierUnknown
	}
	t, ok := hOSTRESOURCESMIBColumnTiers[col.Key()]
	if !ok {
		return snmp.TierUnknown
	}
	return t
}

// HrSWInstalledTableIndicator is the scalar change indicator for hrSWInstalledTable.
// The Watcher Gets hrSWInstalledLastChange on each tick; when the value
// advances the full table is walked and diffed against the snapshot.
// See [snmp.NewScalarIndicator] and [snmp.Watcher] for the contract.
// Discovered by mibgen structural rule: scalar named after the table plus an indicator suffix.
var HrSWInstalledTableIndicator = snmp.MustChangeIndicator(snmp.NewScalarIndicator(snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 1), snmp.KindTimeTicks, []snmp.OID{snmp.MustOID(1, 3, 6, 1, 2, 1, 25, 6, 3)}))
