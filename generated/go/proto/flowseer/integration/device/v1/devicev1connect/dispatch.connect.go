// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: flowseer/integration/device/v1/dispatch.proto

// The two Connect calls that carry the execution envelope between central
// and the edge hosting an integration: the server stream central writes
// dispatches to, and the unary report the edge answers with. Every message
// names its device by id, since one stream serves every device an edge
// hosts.
package devicev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "go.aledante.io/FlowSeer/generated/go/proto/flowseer/integration/device/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// DispatchServiceName is the fully-qualified name of the DispatchService service.
	DispatchServiceName = "flowseer.integration.device.v1.DispatchService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// DispatchServiceSubscribeProcedure is the fully-qualified name of the DispatchService's Subscribe
	// RPC.
	DispatchServiceSubscribeProcedure = "/flowseer.integration.device.v1.DispatchService/Subscribe"
	// DispatchServiceReportProcedure is the fully-qualified name of the DispatchService's Report RPC.
	DispatchServiceReportProcedure = "/flowseer.integration.device.v1.DispatchService/Report"
)

// DispatchServiceClient is a client for the flowseer.integration.device.v1.DispatchService service.
type DispatchServiceClient interface {
	// Opened by an edge with its assertion and held open; central writes
	// every message it owes that edge's devices to it, resending while a
	// message stays unconfirmed, and a closed stream leaves what is owed in
	// central's record for the next open.
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.SubscribeResponse], error)
	// One report from the edge: a result, an acknowledgement, a refusal, or
	// a device onboarded. Central applies it to the device's record and
	// answers once the write is durable.
	Report(context.Context, *connect.Request[v1.ReportRequest]) (*connect.Response[v1.ReportResponse], error)
}

// NewDispatchServiceClient constructs a client for the
// flowseer.integration.device.v1.DispatchService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewDispatchServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) DispatchServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	dispatchServiceMethods := v1.File_flowseer_integration_device_v1_dispatch_proto.Services().ByName("DispatchService").Methods()
	return &dispatchServiceClient{
		subscribe: connect.NewClient[v1.SubscribeRequest, v1.SubscribeResponse](
			httpClient,
			baseURL+DispatchServiceSubscribeProcedure,
			connect.WithSchema(dispatchServiceMethods.ByName("Subscribe")),
			connect.WithClientOptions(opts...),
		),
		report: connect.NewClient[v1.ReportRequest, v1.ReportResponse](
			httpClient,
			baseURL+DispatchServiceReportProcedure,
			connect.WithSchema(dispatchServiceMethods.ByName("Report")),
			connect.WithClientOptions(opts...),
		),
	}
}

// dispatchServiceClient implements DispatchServiceClient.
type dispatchServiceClient struct {
	subscribe *connect.Client[v1.SubscribeRequest, v1.SubscribeResponse]
	report    *connect.Client[v1.ReportRequest, v1.ReportResponse]
}

// Subscribe calls flowseer.integration.device.v1.DispatchService.Subscribe.
func (c *dispatchServiceClient) Subscribe(ctx context.Context, req *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.SubscribeResponse], error) {
	return c.subscribe.CallServerStream(ctx, req)
}

// Report calls flowseer.integration.device.v1.DispatchService.Report.
func (c *dispatchServiceClient) Report(ctx context.Context, req *connect.Request[v1.ReportRequest]) (*connect.Response[v1.ReportResponse], error) {
	return c.report.CallUnary(ctx, req)
}

// DispatchServiceHandler is an implementation of the flowseer.integration.device.v1.DispatchService
// service.
type DispatchServiceHandler interface {
	// Opened by an edge with its assertion and held open; central writes
	// every message it owes that edge's devices to it, resending while a
	// message stays unconfirmed, and a closed stream leaves what is owed in
	// central's record for the next open.
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.SubscribeResponse]) error
	// One report from the edge: a result, an acknowledgement, a refusal, or
	// a device onboarded. Central applies it to the device's record and
	// answers once the write is durable.
	Report(context.Context, *connect.Request[v1.ReportRequest]) (*connect.Response[v1.ReportResponse], error)
}

// NewDispatchServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewDispatchServiceHandler(svc DispatchServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	dispatchServiceMethods := v1.File_flowseer_integration_device_v1_dispatch_proto.Services().ByName("DispatchService").Methods()
	dispatchServiceSubscribeHandler := connect.NewServerStreamHandler(
		DispatchServiceSubscribeProcedure,
		svc.Subscribe,
		connect.WithSchema(dispatchServiceMethods.ByName("Subscribe")),
		connect.WithHandlerOptions(opts...),
	)
	dispatchServiceReportHandler := connect.NewUnaryHandler(
		DispatchServiceReportProcedure,
		svc.Report,
		connect.WithSchema(dispatchServiceMethods.ByName("Report")),
		connect.WithHandlerOptions(opts...),
	)
	return "/flowseer.integration.device.v1.DispatchService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case DispatchServiceSubscribeProcedure:
			dispatchServiceSubscribeHandler.ServeHTTP(w, r)
		case DispatchServiceReportProcedure:
			dispatchServiceReportHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedDispatchServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedDispatchServiceHandler struct{}

func (UnimplementedDispatchServiceHandler) Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.SubscribeResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("flowseer.integration.device.v1.DispatchService.Subscribe is not implemented"))
}

func (UnimplementedDispatchServiceHandler) Report(context.Context, *connect.Request[v1.ReportRequest]) (*connect.Response[v1.ReportResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("flowseer.integration.device.v1.DispatchService.Report is not implemented"))
}
