package lanehost

// Exported for tests. A formatted divergence is its own output — what an
// operator reads — so it is asserted directly rather than through the log
// record that carries it.
var DivergedFields = divergedFields
