edition = "2024";

package flowseer.net.addr.v1;

// Negative fixture for spec/proto/layering_test.go: the ascending-order rule,
// the one R11 exists for. net/addr is index 0 and net/l2 is index 1, so this
// import crosses no special boundary — it is caught only by the general rule,
// which would otherwise ship unexercised.
import "flowseer/net/l2/v1/vlan.proto";

message ImportsUpward {}
