edition = "2024";

package flowseer.net.addr.v1;

// Negative fixture for spec/proto/layering_test.go: the ascending-order rule.
// net/addr is the bottom layer and net/l2 sits one above it, so this import
// crosses no special boundary — it is caught only by the general rule, which
// would otherwise ship unexercised.
import "flowseer/net/l2/v1/vlan.proto";

message ImportsUpward {}
