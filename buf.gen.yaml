version: v2

# Remove the previous output before generating, so a proto file that goes
# away takes its generated Go with it instead of leaving an orphan behind
# that still compiles.
clean: true

managed:
  enabled: true
  disable:
    - file_option: go_package_prefix
      module: buf.build/bufbuild/protovalidate
    - file_option: go_package_prefix
      module: buf.build/googleapis/googleapis
  override:
    - file_option: go_package_prefix
      value: go.aledante.io/FlowSeer/generated/go/proto

plugins:
  - remote: buf.build/protocolbuffers/go
    out: generated/go/proto
    opt: paths=source_relative
  - remote: buf.build/connectrpc/go
    out: generated/go/proto
    opt: paths=source_relative